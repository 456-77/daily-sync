# Daily Sync M1 实施文档 —— 服务端骨架（注册 / 登录 / JWT）

> 目标：把 Spring Boot 3 + MyBatis-Plus + MySQL 的最小可用后端跑起来，
> 实现多用户的注册、登录、令牌刷新和「获取当前用户」接口。
> 完成后你就拥有了一个能注册登录、能被 JWT 保护的标准后端骨架，
> M2 的数据同步接口直接往这个骨架上加即可。
>
> 本文档所有代码均可直接复制使用；本机命令在 Git Bash（Windows）中执行，
> 服务器上的操作通过 SSH 连接云服务器 120.26.58.22 执行。

---

## 0. 本期范围

**做什么：**

- Maven 多模块仓库的 `backend/` 骨架（前端、插件端后续里程碑再加目录）
- MySQL 数据库与用户准备（用云服务器 120.26.58.22 上已运行的 MySQL 8.0.42，Docker 容器部署）
- 注册 `POST /api/v1/auth/register`（支持邀请码开关）
- 登录 `POST /api/v1/auth/login`（双令牌：access 2 小时 + refresh 30 天）
- 刷新 `POST /api/v1/auth/refresh`（refresh token 一次性轮换）
- 当前用户 `GET /api/v1/me`（受 JWT 拦截器保护，用来验证鉴权链路）
- 健康检查 `GET /healthz`

**不做什么（留给后续里程碑）：**

- ❌ 笔记本 / 每日记录表与同步接口（M2）
- ❌ React 前端（M4）
- ❌ Spring Security 完整框架——M1 用「拦截器 + ThreadLocal」手写鉴权，
  逻辑全部可见，学习价值更高；以后想换 Spring Security 也是局部重构

**部署形态说明：**

- 后端最终要部署到云服务器 `120.26.58.22` 上（M6 落地）；M1 阶段先在本机运行，
  数据库直连服务器上的 MySQL
- 服务器上的 MySQL 目前已由 Docker 容器承载（端口 3306 已对公网放开）；
  后续会把服务器上的服务统一用 docker-compose 管理，本期不动

---

## 1. 前置环境

### 1.1 JDK 17

Spring Boot 3 最低要求 Java 17。本机已有 `H:\develop\jdk-23`（Java 23）且在 PATH 里，
Spring Boot 3.5 支持 Java 17~24，直接用即可，无需再装：

```bash
java -version
# 预期：java version "23.0.1"
```

> 工程 pom 里 `java.version` 仍写 17（字节码目标版本），用 JDK 23 编译完全兼容。
> 换了机器没有 JDK 时，再 `winget install EclipseAdoptium.Temurin.17.JDK` 装 17+ 均可。

### 1.2 Maven + 阿里云镜像

Maven 已装好：`H:\develop\apache-maven-3.9.16`，`bin` 已在 PATH（验证 `mvn -version`）。
还差最后一步——配置阿里云镜像（国内下载依赖速度差几十倍）。
当前没有用户级配置，创建 `C:\Users\一切随缘\.m2\settings.xml`，内容：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
  <!-- 复用已有本地仓库（H:\develop\repository 里已缓存大量依赖，能省不少下载）；
       不想复用就删掉这一行，用默认的 ~/.m2/repository -->
  <localRepository>H:/develop/repository</localRepository>
  <mirrors>
    <mirror>
      <id>aliyunmaven</id>
      <mirrorOf>*</mirrorOf>
      <name>阿里云公共仓库</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
```

验证：

```bash
mvn -version
```

> 用 IDEA 内置 Maven 也行，但 `settings.xml` 的阿里云镜像一样要配（IDEA 设置里可指定使用自定义 settings.xml）。

### 1.3 IDE

IntelliJ IDEA（Community 免费版足够）：
- `File → Project Structure → SDK` 选 `H:\develop\jdk-23`（已装好）
- 装上 Lombok 插件（2023+ 版本自带），并开启 `Settings → Build → Compiler → Annotation Processors → Enable annotation processing`

> 本期可以完全不打开 IDEA：命令行 `mvn spring-boot:run` 就能跑通全部验收，
> 需要断点调试时再用 IDEA 打开也不迟。

### 1.4 准备数据库（云服务器 120.26.58.22，MySQL 已就绪）

服务器现状——MySQL 已经在 Docker 容器里运行着，**不需要再安装**：

| 项 | 值 |
| --- | --- |
| 服务器 | 阿里云 ECS，CentOS 7，公网 IP `120.26.58.22`（固定，不受 DHCP 影响） |
| SSH | 用户 `root`，密码 `@tjj123456` |
| MySQL | Docker 容器运行，版本 8.0.42，宿主机 3306 已映射且公网可访问 |
| MySQL root 密码 | `tjj123456` |
| Redis | 服务器上也有（密码同为 `tjj123456`），M1 用不到，后续里程碑再说 |

> ⚠️ 密码直接写在本文档里只是图方便，**不要把本文档推到公开仓库**；
> 上线前应改用环境变量注入，并把安全组里 3306 的来源 IP 收窄。

用下面任一方式连上 MySQL 后，整段执行 SQL——专门建一个应用账号，不用 root 跑应用
（语句全部幂等，重复执行不报错）：

**方式 A：Windows 本机直连**（`H:\develop\MySQL\MySQL Server 8.0\bin` 已在 PATH，最方便）：

```bash
mysql -h120.26.58.22 -uroot -p
# 输入密码：tjj123456
```

**方式 B：SSH 到服务器上执行**：

```bash
ssh root@120.26.58.22
mysql -h127.0.0.1 -uroot -p     # docker-proxy 已把容器 3306 映射到宿主机
```

```sql
CREATE DATABASE IF NOT EXISTS daily_sync DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'daily_sync'@'%' IDENTIFIED BY 'DailySync_2026';
GRANT ALL PRIVILEGES ON daily_sync.* TO 'daily_sync'@'%';
FLUSH PRIVILEGES;
```

> 应用账号密码 `DailySync_2026` 想换的话自己改，后文 application.yml 里保持一致即可。

### 1.5 验证 Windows 能连到服务器的 MySQL

在 Windows 的 **PowerShell** 里执行：

```powershell
Test-NetConnection 120.26.58.22 -Port 3306
```

看到 `TcpTestSucceeded : True` 即通（编写本文时已验证可通）。如果失败，
到阿里云控制台 → ECS → 安全组 → 入方向规则，放行 3306 端口；
服务器自身的 firewalld 不用动（Docker 已完成端口映射）。

---

## 2. 仓库与项目结构

```
H:\develop\project\daily-sync\  ← 仓库根（git init；本文档就在这里）
├── M1-实施文档.md              ← 本文档
├── backend/                    ← Spring Boot 工程（本期）
├── frontend/                   ← React 工程（M4 再建）
├── docker-compose.yml          ← 服务器服务统一容器化时用（后续再建）
├── Jenkinsfile                 ← M6 再建
└── .gitignore
```

在 `H:\develop\project\daily-sync` 下初始化仓库，并创建 `.gitignore`：

```bash
cd /h/develop/project/daily-sync
git init
```

`.gitignore` 内容：

```gitignore
# Java / Maven
backend/target/
*.class

# IDE
.idea/
*.iml
.vscode/

# 前端（M4 用）
frontend/node_modules/
frontend/dist/

# 环境变量与本地配置
.env
*.local.yml
```

---

## 3. 创建 Spring Boot 工程

不用手动建目录，直接在 `H:\develop\project\daily-sync` 下创建 `backend/pom.xml`（全文如下），
IDEA 打开后会自动按 Maven 工程导入。

**版本说明（写本文时点的稳定组合，能启动就别追新）：**
- Spring Boot **3.5.4**（3.x 系列需要 Java 17+）
- MyBatis-Plus **3.5.12**，必须用 `mybatis-plus-spring-boot3-starter`
  （Boot 3 专用 starter；**不要用** `mybatis-plus-boot-starter`，那是 Boot 2 的）
- JJWT **0.12.6**（0.12 起 API 大改，网上旧教程的写法会编译不过，以本文代码为准）

`backend/pom.xml`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.4</version>
    <relativePath/>
  </parent>

  <groupId>com.dailysync</groupId>
  <artifactId>daily-sync-backend</artifactId>
  <version>0.1.0</version>
  <name>daily-sync-backend</name>

  <properties>
    <java.version>17</java.version>
    <mybatis-plus.version>3.5.12</mybatis-plus.version>
    <jjwt.version>0.12.6</jjwt.version>
  </properties>

  <dependencies>
    <!-- Web 与参数校验 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- MyBatis-Plus（Spring Boot 3 专用 starter） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>

    <!-- MySQL 驱动（版本由 Spring Boot 管理） -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- 只用 bcrypt，不引入完整 Spring Security -->
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-crypto</artifactId>
    </dependency>

    <!-- JWT（三件套，impl/jackson 仅运行期需要） -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Lombok：自动生成 getter/构造器 -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
```

---

## 4. 配置文件

`backend/src/main/resources/application.yml`：

```yaml
server:
  port: 8080

spring:
  application:
    name: daily-sync
  datasource:
    # 云服务器 120.26.58.22 上的 MySQL（Docker 容器，端口已对公网放开）；
    # allowPublicKeyRetrieval 是 MySQL 8 caching_sha2_password 首连必需
    url: jdbc:mysql://120.26.58.22:3306/daily_sync?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: daily_sync
    password: ${DB_PASSWORD:DailySync_2026}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # 开发期把 SQL 打到控制台，方便学习观察；上生产前删掉
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false

daily-sync:
  # JWT 签名密钥：必须 >= 32 字节。生产环境用环境变量覆盖，不要用默认值
  jwt-secret: ${JWT_SECRET:dev-only-secret-please-change-me-0123456789abcdef}
  jwt-access-ttl-minutes: 120
  # 注册邀请码：留空 = 开放注册；配置后注册必须携带
  invite-code: ${INVITE_CODE:}
```

> 敏感信息（数据库密码、JWT 密钥）都用 `${ENV:默认值}` 形式：本地开发有默认值即可跑，
> 部署时用环境变量覆盖，配置文件本身可以安全提交。

---

## 5. 数据库表（M1 只需要两张）

用 1.4 的任一方式连上服务器 MySQL 执行（或用 DataGrip/Navicat 直连 `120.26.58.22`）：

```bash
# Windows 本机直连
mysql -h120.26.58.22 -u daily_sync -p daily_sync
```

```sql
CREATE TABLE users (
  id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
  username      VARCHAR(32)  NOT NULL,
  password_hash VARCHAR(100) NOT NULL,                -- bcrypt 哈希
  status        TINYINT      NOT NULL DEFAULT 1,      -- 1 正常 0 禁用
  created_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE refresh_tokens (
  id         BIGINT      PRIMARY KEY AUTO_INCREMENT,
  user_id    BIGINT      NOT NULL,
  token_hash CHAR(64)    NOT NULL,                    -- SHA-256(refresh 明文)，不存明文
  expires_at DATETIME    NOT NULL,
  created_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_token_hash (token_hash),
  KEY idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 6. 代码实现

Java 源码根目录：`backend/src/main/java/com/dailysync/`。
共 18 个文件，按包列出，每个都可以整文件复制。

```
com.dailysync
├── DailySyncApplication.java
├── common/     ApiResponse / BizException / GlobalExceptionHandler
├── config/     AppConfig / WebMvcConfig
├── auth/       JwtUtil / UserContext / AuthInterceptor
├── entity/     User / RefreshToken
├── mapper/     UserMapper / RefreshTokenMapper
├── dto/        RegisterRequest / LoginRequest / RefreshRequest / TokenResponse / UserInfoResponse
├── service/    AuthService
└── controller/ AuthController / MeController / HealthController
```

### 6.1 启动类

`DailySyncApplication.java`：

```java
package com.dailysync;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.dailysync.mapper")
public class DailySyncApplication {
    public static void main(String[] args) {
        SpringApplication.run(DailySyncApplication.class, args);
    }
}
```

### 6.2 统一响应与全局异常

`common/ApiResponse.java` —— 所有接口统一返回 `{code, message, data}`：

```java
package com.dailysync.common;

public record ApiResponse<T>(int code, String message, T data) {
    public static <T> ApiResponse<T> ok(T data) {
        return new ApiResponse<>(0, "ok", data);
    }

    public static ApiResponse<Void> error(int code, String message) {
        return new ApiResponse<>(code, message, null);
    }
}
```

`common/BizException.java` —— 业务异常（带 HTTP 状态码）：

```java
package com.dailysync.common;

import lombok.Getter;
import org.springframework.http.HttpStatus;

@Getter
public class BizException extends RuntimeException {
    private final HttpStatus status;

    public BizException(HttpStatus status, String message) {
        super(message);
        this.status = status;
    }
}
```

`common/GlobalExceptionHandler.java`：

```java
package com.dailysync.common;

import lombok.extern.slf4j.Slf4j;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {

    /** 业务异常：用异常里带的 HTTP 状态码 */
    @ExceptionHandler(BizException.class)
    public ResponseEntity<ApiResponse<Void>> handleBiz(BizException e) {
        return ResponseEntity.status(e.getStatus()).body(ApiResponse.error(e.getStatus().value(), e.getMessage()));
    }

    /** 参数校验失败：取第一个字段的错误信息 */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ApiResponse<Void>> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
                .findFirst()
                .map(f -> f.getField() + ": " + f.getDefaultMessage())
                .orElse("参数错误");
        return ResponseEntity.badRequest().body(ApiResponse.error(400, msg));
    }

    /** 兜底：未知异常不暴露堆栈给前端，完整堆栈进日志 */
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ApiResponse<Void>> handleUnknown(Exception e) {
        log.error("未处理异常", e);
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body(ApiResponse.error(500, "服务器内部错误"));
    }
}
```

### 6.3 配置类

`config/AppConfig.java` —— bcrypt 编码器 Bean：

```java
package com.dailysync.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
public class AppConfig {

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

`config/WebMvcConfig.java` —— 注册 JWT 拦截器，放行认证接口：

```java
package com.dailysync.config;

import com.dailysync.auth.AuthInterceptor;
import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
@RequiredArgsConstructor
public class WebMvcConfig implements WebMvcConfigurer {

    private final AuthInterceptor authInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(authInterceptor)
                .addPathPatterns("/api/v1/**")
                .excludePathPatterns("/api/v1/auth/**");
    }
}
```

### 6.4 鉴权三件套

`auth/UserContext.java` —— 当前登录用户的 ThreadLocal 上下文：

```java
package com.dailysync.auth;

public final class UserContext {
    private static final ThreadLocal<Long> USER_ID = new ThreadLocal<>();
    private static final ThreadLocal<String> USERNAME = new ThreadLocal<>();

    private UserContext() {}

    public static void set(Long userId, String username) {
        USER_ID.set(userId);
        USERNAME.set(username);
    }

    public static Long userId() {
        return USER_ID.get();
    }

    public static String username() {
        return USERNAME.get();
    }

    /** 请求结束后必须清理，防止线程池复用导致串号 */
    public static void clear() {
        USER_ID.remove();
        USERNAME.remove();
    }
}
```

`auth/JwtUtil.java` —— 签发与解析 access token（JJWT 0.12 API）：

```java
package com.dailysync.auth;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;

@Component
public class JwtUtil {

    @Value("${daily-sync.jwt-secret}")
    private String secret;

    @Value("${daily-sync.jwt-access-ttl-minutes:120}")
    private long accessTtlMinutes;

    private SecretKey key() {
        return Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
    }

    public String createAccessToken(Long userId, String username) {
        Date now = new Date();
        Date expiry = new Date(now.getTime() + accessTtlMinutes * 60_000);
        return Jwts.builder()
                .subject(String.valueOf(userId))
                .claim("username", username)
                .issuedAt(now)
                .expiration(expiry)
                .signWith(key())
                .compact();
    }

    /** 解析并验签；过期或伪造会抛 JwtException，由拦截器统一转 401 */
    public Claims parse(String token) {
        return Jwts.parser().verifyWith(key()).build().parseSignedClaims(token).getPayload();
    }

    public long accessTtlSeconds() {
        return accessTtlMinutes * 60;
    }
}
```

`auth/AuthInterceptor.java` —— 校验 Authorization 头，把用户信息放进 ThreadLocal：

```java
package com.dailysync.auth;

import com.dailysync.common.ApiResponse;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.jsonwebtoken.Claims;
import io.jsonwebtoken.JwtException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;
import org.springframework.web.method.HandlerMethod;
import org.springframework.web.servlet.HandlerInterceptor;

import java.nio.charset.StandardCharsets;

@Component
@RequiredArgsConstructor
public class AuthInterceptor implements HandlerInterceptor {

    private final JwtUtil jwtUtil;
    private final ObjectMapper objectMapper;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler)
            throws Exception {
        // 放行非 Controller 请求（静态资源、异常转发等）
        if (!(handler instanceof HandlerMethod)) {
            return true;
        }

        String header = request.getHeader("Authorization");
        if (header == null || !header.startsWith("Bearer ")) {
            return reject(response);
        }
        try {
            Claims claims = jwtUtil.parse(header.substring(7));
            UserContext.set(Long.valueOf(claims.getSubject()), claims.get("username", String.class));
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return reject(response);
        }
    }

    private boolean reject(HttpServletResponse response) throws Exception {
        response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
        response.setContentType("application/json;charset=UTF-8");
        byte[] body = objectMapper.writeValueAsBytes(ApiResponse.error(401, "未登录或令牌已失效"));
        response.getOutputStream().write(body);
        response.setContentLength(body.length);
        response.setCharacterEncoding(StandardCharsets.UTF_8.name());
        return false;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response,
                                Object handler, Exception ex) {
        UserContext.clear();
    }
}
```

### 6.5 实体与 Mapper

`entity/User.java`：

```java
package com.dailysync.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@TableName("users")
public class User {
    @TableId(type = IdType.AUTO)
    private Long id;
    private String username;
    private String passwordHash;
    private Integer status;
    private LocalDateTime createdAt;
}
```

`entity/RefreshToken.java`：

```java
package com.dailysync.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@TableName("refresh_tokens")
public class RefreshToken {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long userId;
    private String tokenHash;
    private LocalDateTime expiresAt;
    private LocalDateTime createdAt;
}
```

`mapper/UserMapper.java`：

```java
package com.dailysync.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dailysync.entity.User;

public interface UserMapper extends BaseMapper<User> {
}
```

`mapper/RefreshTokenMapper.java`：

```java
package com.dailysync.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dailysync.entity.RefreshToken;

public interface RefreshTokenMapper extends BaseMapper<RefreshToken> {
}
```

### 6.6 DTO

`dto/RegisterRequest.java`：

```java
package com.dailysync.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import lombok.Data;

@Data
public class RegisterRequest {
    @NotBlank
    @Pattern(regexp = "^[a-zA-Z0-9_]{3,32}$", message = "用户名只能包含字母、数字、下划线，长度 3-32")
    private String username;

    @NotBlank
    @Size(min = 8, max = 64, message = "密码长度需要 8-64 位")
    private String password;

    /** 邀请码：服务端配置了邀请码时必填 */
    @Size(max = 64)
    private String inviteCode;
}
```

`dto/LoginRequest.java`：

```java
package com.dailysync.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;

@Data
public class LoginRequest {
    @NotBlank
    private String username;

    @NotBlank
    private String password;
}
```

`dto/RefreshRequest.java`：

```java
package com.dailysync.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;

@Data
public class RefreshRequest {
    @NotBlank
    private String refreshToken;
}
```

`dto/TokenResponse.java`：

```java
package com.dailysync.dto;

import lombok.AllArgsConstructor;
import lombok.Data;

@Data
@AllArgsConstructor
public class TokenResponse {
    private String accessToken;
    private String refreshToken;
    /** access token 有效期（秒），前端用它提前刷新 */
    private long expiresIn;
}
```

`dto/UserInfoResponse.java`：

```java
package com.dailysync.dto;

import lombok.AllArgsConstructor;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@AllArgsConstructor
public class UserInfoResponse {
    private Long id;
    private String username;
    private LocalDateTime createdAt;
}
```

### 6.7 核心业务 AuthService

`service/AuthService.java`：

```java
package com.dailysync.service;

import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.dailysync.common.BizException;
import com.dailysync.dto.*;
import com.dailysync.entity.RefreshToken;
import com.dailysync.entity.User;
import com.dailysync.mapper.RefreshTokenMapper;
import com.dailysync.mapper.UserMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.http.HttpStatus;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.security.SecureRandom;
import java.time.LocalDateTime;
import java.util.HexFormat;

@Service
@RequiredArgsConstructor
public class AuthService {

    private static final long REFRESH_TTL_DAYS = 30;
    private static final SecureRandom RANDOM = new SecureRandom();

    private final UserMapper userMapper;
    private final RefreshTokenMapper refreshTokenMapper;
    private final JwtUtil jwtUtil;
    private final PasswordEncoder passwordEncoder;

    @Value("${daily-sync.invite-code:}")
    private String inviteCode;

    @Transactional
    public TokenResponse register(RegisterRequest req) {
        if (!inviteCode.isBlank() && !inviteCode.equals(req.getInviteCode())) {
            throw new BizException(HttpStatus.BAD_REQUEST, "邀请码错误");
        }
        Long count = userMapper.selectCount(
                Wrappers.<User>lambdaQuery().eq(User::getUsername, req.getUsername()));
        if (count != null && count > 0) {
            throw new BizException(HttpStatus.CONFLICT, "用户名已被占用");
        }

        User user = new User();
        user.setUsername(req.getUsername());
        user.setPasswordHash(passwordEncoder.encode(req.getPassword()));
        user.setStatus(1);
        userMapper.insert(user);

        return issueTokens(user);
    }

    public TokenResponse login(LoginRequest req) {
        User user = userMapper.selectOne(
                Wrappers.<User>lambdaQuery().eq(User::getUsername, req.getUsername()));
        // 用户不存在与密码错误返回同一句话，不给撞库者提示
        if (user == null || !passwordEncoder.matches(req.getPassword(), user.getPasswordHash())) {
            throw new BizException(HttpStatus.UNAUTHORIZED, "用户名或密码错误");
        }
        if (user.getStatus() != 1) {
            throw new BizException(HttpStatus.FORBIDDEN, "账号已被禁用");
        }
        return issueTokens(user);
    }

    /** 用 refresh token 换新令牌对；旧 refresh 一次性作废（轮换） */
    @Transactional
    public TokenResponse refresh(RefreshRequest req) {
        String hash = sha256Hex(req.getRefreshToken());
        RefreshToken stored = refreshTokenMapper.selectOne(
                Wrappers.<RefreshToken>lambdaQuery().eq(RefreshToken::getTokenHash, hash));
        if (stored == null || stored.getExpiresAt().isBefore(LocalDateTime.now())) {
            throw new BizException(HttpStatus.UNAUTHORIZED, "refresh token 无效或已过期");
        }
        refreshTokenMapper.deleteById(stored.getId());

        User user = userMapper.selectById(stored.getUserId());
        if (user == null || user.getStatus() != 1) {
            throw new BizException(HttpStatus.UNAUTHORIZED, "账号不存在或已被禁用");
        }
        return issueTokens(user);
    }

    /** 签发 access + refresh：refresh 明文只出现这一次，库里只存哈希 */
    private TokenResponse issueTokens(User user) {
        byte[] bytes = new byte[32];
        RANDOM.nextBytes(bytes);
        String refreshToken = HexFormat.of().formatHex(bytes);

        RefreshToken entity = new RefreshToken();
        entity.setUserId(user.getId());
        entity.setTokenHash(sha256Hex(refreshToken));
        entity.setExpiresAt(LocalDateTime.now().plusDays(REFRESH_TTL_DAYS));
        refreshTokenMapper.insert(entity);

        return new TokenResponse(
                jwtUtil.createAccessToken(user.getId(), user.getUsername()),
                refreshToken,
                jwtUtil.accessTtlSeconds());
    }

    private String sha256Hex(String input) {
        try {
            byte[] digest = MessageDigest.getInstance("SHA-256")
                    .digest(input.getBytes(StandardCharsets.UTF_8));
            return HexFormat.of().formatHex(digest);
        } catch (Exception e) {
            throw new IllegalStateException("SHA-256 不可用", e);
        }
    }
}
```

### 6.8 Controller

`controller/AuthController.java`：

```java
package com.dailysync.controller;

import com.dailysync.common.ApiResponse;
import com.dailysync.dto.*;
import com.dailysync.service.AuthService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/v1/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    @PostMapping("/register")
    public ApiResponse<TokenResponse> register(@Valid @RequestBody RegisterRequest req) {
        return ApiResponse.ok(authService.register(req));
    }

    @PostMapping("/login")
    public ApiResponse<TokenResponse> login(@Valid @RequestBody LoginRequest req) {
        return ApiResponse.ok(authService.login(req));
    }

    @PostMapping("/refresh")
    public ApiResponse<TokenResponse> refresh(@Valid @RequestBody RefreshRequest req) {
        return ApiResponse.ok(authService.refresh(req));
    }
}
```

`controller/MeController.java` —— 受保护的接口，验证鉴权链路：

```java
package com.dailysync.controller;

import com.dailysync.auth.UserContext;
import com.dailysync.common.ApiResponse;
import com.dailysync.common.BizException;
import com.dailysync.dto.UserInfoResponse;
import com.dailysync.entity.User;
import com.dailysync.mapper.UserMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequiredArgsConstructor
public class MeController {

    private final UserMapper userMapper;

    @GetMapping("/api/v1/me")
    public ApiResponse<UserInfoResponse> me() {
        User user = userMapper.selectById(UserContext.userId());
        if (user == null) {
            throw new BizException(HttpStatus.UNAUTHORIZED, "用户不存在");
        }
        return ApiResponse.ok(new UserInfoResponse(user.getId(), user.getUsername(), user.getCreatedAt()));
    }
}
```

`controller/HealthController.java`：

```java
package com.dailysync.controller;

import java.util.Map;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HealthController {

    @GetMapping("/healthz")
    public Map<String, String> health() {
        return Map.of("status", "ok");
    }
}
```

---

## 7. 运行

IDEA 里直接运行 `DailySyncApplication`，或命令行：

```bash
cd /h/develop/project/daily-sync/backend
mvn spring-boot:run
```

看到 `Started DailySyncApplication` 且没有红色堆栈即成功。
启动时控制台会打印 MyBatis-Plus 的 SQL 日志（学习期保留，上线前删掉配置里的 `log-impl`）。

---

## 8. 验证（Git Bash 里逐步执行）

**① 健康检查：**

```bash
curl -s http://localhost:8080/healthz
# 预期：{"status":"ok"}
```

**② 注册（第一次，应成功）：**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"shuiyi","password":"yourPassword123"}'
# 预期：{"code":0,"message":"ok","data":{"accessToken":"eyJ...","refreshToken":"64位十六进制","expiresIn":7200}}
```

**③ 注册同名（应被拒绝）：**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"shuiyi","password":"yourPassword123"}'
# 预期：HTTP 409，{"code":409,"message":"用户名已被占用","data":null}
```

**④ 登录：**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"shuiyi","password":"yourPassword123"}'
# 复制返回里的 accessToken 到下一句
```

**⑤ 带 token 访问受保护接口：**

```bash
curl -s http://localhost:8080/api/v1/me \
  -H "Authorization: Bearer 粘贴你的accessToken"
# 预期：{"code":0,"message":"ok","data":{"id":1,"username":"shuiyi","createdAt":"..."}}
```

**⑥ 不带 token（应被拦截）：**

```bash
curl -s http://localhost:8080/api/v1/me
# 预期：HTTP 401，{"code":401,"message":"未登录或令牌已失效","data":null}
```

**⑦ 用 refreshToken 换新令牌对：**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken":"粘贴你登录时拿到的refreshToken"}'
# 预期：新的 accessToken + refreshToken（旧的已作废，再用它会 401）
```

---

## 9. 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 启动报 `Public Key Retrieval is not allowed` | MySQL 8 密码插件首连问题，JDBC URL 已带 `allowPublicKeyRetrieval=true`，确认没删 |
| 通讯失败/连不上数据库 | 先跑 1.5 的端口连通性测试；确认服务器 IP 仍是 120.26.58.22、阿里云安全组仍放行 3306 |
| 拉依赖极慢/失败 | `settings.xml` 阿里云镜像没配好；删除本地仓库（`H:\develop\repository`）里报错的目录后重试 |
| MyBatis 相关 `ClassNotFoundException` | 版本组合问题：确认用的是 `mybatis-plus-spring-boot3-starter` 而不是 `mybatis-plus-boot-starter` |
| JJWT 相关方法不存在 | 用了旧版 API 的写法；本文代码基于 0.12.6，`Jwts.parser().verifyWith(key())` 才是 0.12 写法 |
| `me` 接口 401 但 token 看着正常 | 检查 Header 格式必须是 `Authorization: Bearer <token>`（Bearer 后有一个空格） |
| 时间字段差 8 小时 | JDBC URL 里的 `serverTimezone=Asia/Shanghai` 丢了；服务器系统时区也确认一下（`timedatectl`） |
| Lombok 报 getter 不存在 | IDEA 未开启 annotation processing（见 1.3） |

---

## 10. 验收清单与下一步

M1 完成标准（全部打勾）：

- [ ] `mvn spring-boot:run` 正常启动，连接云服务器（120.26.58.22）上的 MySQL
- [ ] 第 8 节 ①~⑦ 全部符合预期
- [ ] 数据库里能看到 `users` 新增一行（`password_hash` 是 `$2a$10$...` 的 bcrypt 串）和 `refresh_tokens` 记录
- [ ] 代码已提交：`git add -A && git commit -m "M1: 服务端骨架（注册/登录/JWT）"`

**M2 预告**：`vaults` / `daily_records` 建表、同步令牌（sync_tokens）管理接口、
`POST /api/v1/sync` 幂等推送（用真实 token 替换 dev secret，开始准备环境变量管理）。
文档完成后接口可以直接挂在本骨架上，AuthInterceptor 的排除列表里加上 `/api/v1/sync`（它用同步令牌鉴权，不走 JWT）。

**部署路线**：后端最终部署到云服务器 120.26.58.22（M6，Jenkins + Docker 落地）。
服务器上的服务后续会统一用 docker-compose 管理——MySQL 现在已经是容器形态，
届时把后端也容器化接进去即可；M1 阶段先本机运行、直连服务器数据库。
