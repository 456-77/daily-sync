# Daily Sync M2 实施文档 —— 数据同步（仓库 / 记录 / 同步令牌）

> 目标：在 M1 骨架上补齐数据同步能力——建三张表（`vaults` / `daily_records` / `sync_tokens`），
> 提供仓库与同步令牌管理接口（JWT 保护），以及 `POST/GET /api/v1/sync` 幂等推送与增量拉取
> （用同步令牌鉴权，替换掉"写死的 dev secret"这种土办法）。
> 完成后服务端就具备完整同步闭环，M3 只需给 `quick-daily-note` 插件接上这两个接口。
>
> 本文档所有代码均可直接复制使用；命令在 Git Bash（Windows）执行，
> 数据库还是云服务器 120.26.58.22 上的 MySQL（M1 已连通）。

---

## 0. 本期范围

**做什么：**

- 三张新表：`vaults`（仓库）、`daily_records`（记录）、`sync_tokens`（同步令牌）
- 仓库与令牌管理接口（JWT 保护）：
  - `POST /api/v1/vaults`、`GET /api/v1/vaults`
  - `POST /api/v1/vaults/{id}/tokens`、`GET /api/v1/vaults/{id}/tokens`、`DELETE /api/v1/vaults/{id}/tokens/{tokenId}`
- 同步接口（同步令牌保护，不走 JWT）：
  - `POST /api/v1/sync` —— 批量幂等推送（含墓碑删除）
  - `GET /api/v1/sync?since=&limit=` —— 增量拉取
- 环境变量管理准备（生成正式 JWT 密钥、`.env` 约定）

**不做什么（留给后续里程碑）：**

- ❌ 插件端接入（M3：设置界面、文件钩子、冲突落地）
- ❌ 附件 / 图片同步（M2 只同步 `.md`；日记里的图片引用原样保存在正文中，附件文件本身不传）
- ❌ 冲突合并界面（M2 服务端语义为"后到者胜"，见 1.3；客户端策略 M3 落地）
- ❌ React 前端（M4）、部署（M6）

**术语约定（后文不再解释）：**

| 术语 | 含义 |
| --- | --- |
| 仓库（vault） | 一个 Obsidian 库。同一用户可有多个仓库（比如"个人库""工作库"），记录按仓库隔离 |
| 记录（record） | 日记文件夹内的一个 `.md` 文件（日记、一天多篇的"日期 名字"、周记都算） |
| 路径（path） | 记录文件在库内的相对路径，如 `diary/2026-09-09.md`，是记录的唯一业务键 |
| 同步令牌 | `dst_` 开头的长随机串，绑定一个仓库，插件用它调 `/api/v1/sync` |

---

## 1. 设计：数据模型与同步协议

> 先把协议说清楚再写代码。这一节是 M3 插件端实现的依据，也是后面所有接口行为的解释。

### 1.1 接口总览

| 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- |
| POST | `/api/v1/vaults` | JWT | 创建仓库 |
| GET | `/api/v1/vaults` | JWT | 我的仓库列表 |
| POST | `/api/v1/vaults/{id}/tokens` | JWT | 签发同步令牌（**明文只返回这一次**） |
| GET | `/api/v1/vaults/{id}/tokens` | JWT | 令牌列表（不含明文） |
| DELETE | `/api/v1/vaults/{id}/tokens/{tokenId}` | JWT | 撤销令牌（立即生效） |
| POST | `/api/v1/sync` | `X-Sync-Token` | 批量幂等推送 |
| GET | `/api/v1/sync?since=&limit=` | `X-Sync-Token` | 增量拉取 |

### 1.2 版本号：拉取游标的核心

每个仓库有一个单调递增的 `version` 计数器（存在 `vaults.version`，从 0 开始）：

- 一次推送中**只要有任意一条记录发生实际变更**（新增、内容变化、删除/恢复），仓库 version +1；
  该批所有变更行打上同一个新版本号。
- 内容没变的推送（哈希相同）是 no-op，**不推进版本号**——这就是幂等：网络重试、插件重复
  推送都不会产生新版本，其他设备也不会被无意义地唤醒。
- 拉取用 `since`（上次已同步到的版本号）做增量：返回 `version > since` 的行。
  客户端把"游标"存在本地，全量同步就是 `since=0`。

**为什么不用时间戳做游标**：客户端时钟不可信、DATETIME 秒级精度会丢同秒内的多条变更、
并发写没有稳定的先后。数据库里的原子计数器一次解决全部问题。

**为什么每个仓库一个计数器而不是全局**：仓库之间互不影响，一个仓库的写入不会让另一个
仓库的客户端白拉一次增量。

### 1.3 同步语义（M2 就是这么简单，诚实说明）

- **后到者胜（last-write-wins）**：同一台设备重复推送相同内容是 no-op；两台设备先后推送
  同一路径的不同内容，服务器保留后到的。个人日记场景下，同一篇日记被两台设备**在同一时间窗内**
  同时编辑的概率极低，M2 不做三路合并。
- **M3 客户端配合策略**（服务端已为此备好信息）：拉取时若本地文件哈希 ≠ 上次同步的哈希
  且服务器也改了它 → 本地直接再推一次（本地胜），或提示用户。细节 M3 文档定。
- **删除 = 墓碑**：删除不是物理删行，而是把该行标记 `deleted=1`（内容清空）。墓碑会随增量
  拉取下发，其他设备据此删本地文件；墓碑永久保留（防"诈尸"：旧设备重新推上来）。
  再次推送同一路径的内容 = 复活该记录。

### 1.4 拉取翻页规则（处理一次推送多条记录共享同一版本号）

一次推送的所有变更行版本号相同，`LIMIT` 可能恰好切在同一个版本组的中间。规则：

- 响应带 `hasMore`（返回条数 == limit 时为 true）。
- `hasMore = false` → 下次 `since = vaultVersion`（响应里的仓库当前版本）。
- `hasMore = true` → 下次 `since = 最后一条记录的 version - 1`，客户端按 `path` 去重
  （后到的覆盖先到的）。重拉会重复收到上一页尾部，去重即可，正确性不受影响。
- 不变式：**单次推送上限 200 条 ≤ 拉取 limit 上限 500**，一个版本组必然能在一页内装下，
  上述规则不会死循环。

### 1.5 同步令牌

- 格式：`dst_` + 32 字节 SecureRandom 的 64 位十六进制，共 68 字符。
- 库里只存 SHA-256 哈希（和 M1 的 refresh token 同一套做法）；明文只在签发响应里出现一次。
- 请求头：`X-Sync-Token: dst_xxx...`。无效 / 已撤销 / 所属账号被禁用 → 401。
- 令牌绑定仓库（不是绑定用户）：一个仓库可签多枚（一台设备一枚，名字备注），撤销单枚
  不影响其他设备。
- 每次成功调用更新 `last_used_at`，方便观察哪台设备还在活跃。

### 1.6 限制（服务端强制）

| 项 | 限制 |
| --- | --- |
| 单次推送条数 | ≤ 200 |
| 单条 content | ≤ 1MB（1,048,576 字符） |
| path 长度 | ≤ 255，且必须以 `.md` 结尾 |
| path 形态 | 相对路径：不以 `/` 开头、不含 `\`、不含 `.` / `..` 路径段 |
| 同一路径 | 在一次推送中只能出现一次（重复直接 400） |
| 拉取 limit | 1~500，默认 200 |

---

## 2. 建表（云服务器 MySQL，三张表全部幂等）

```bash
mysql -h120.26.58.22 -u daily_sync -p daily_sync
```

```sql
-- 仓库：version 是拉取游标计数器
CREATE TABLE IF NOT EXISTS vaults (
  id         BIGINT      PRIMARY KEY AUTO_INCREMENT,
  user_id    BIGINT      NOT NULL,
  name       VARCHAR(64) NOT NULL,
  version    BIGINT      NOT NULL DEFAULT 0,
  created_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_name (user_id, name),
  KEY idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 记录：(vault_id, path) 唯一；deleted=1 即墓碑；version = 写入时的仓库版本号
-- record_date 由服务端从文件名首段解析（YYYY-MM-DD 才认，周记/其他为 NULL），给 M4 前端查询用
CREATE TABLE IF NOT EXISTS daily_records (
  id           BIGINT       PRIMARY KEY AUTO_INCREMENT,
  vault_id     BIGINT       NOT NULL,
  path         VARCHAR(255) NOT NULL,
  content      MEDIUMTEXT   NOT NULL,
  content_hash CHAR(64)     NOT NULL,
  deleted      TINYINT      NOT NULL DEFAULT 0,
  version      BIGINT       NOT NULL,
  record_date  DATE         NULL,
  updated_at   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  created_at   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_vault_path (vault_id, path),
  KEY idx_vault_version (vault_id, version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 同步令牌：只存哈希；status 1 正常 0 已撤销
CREATE TABLE IF NOT EXISTS sync_tokens (
  id           BIGINT      PRIMARY KEY AUTO_INCREMENT,
  vault_id     BIGINT      NOT NULL,
  name         VARCHAR(64) NOT NULL DEFAULT '',
  token_hash   CHAR(64)    NOT NULL,
  status       TINYINT     NOT NULL DEFAULT 1,
  last_used_at DATETIME    NULL,
  created_at   DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_token_hash (token_hash),
  KEY idx_vault (vault_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

> `MEDIUMTEXT` 上限 16MB，对 1MB 的单条限制有充分余量；
> `uk_vault_path` 唯一键是幂等 upsert 的前提。

---

## 3. 代码实现

Java 源码根目录不变：`backend/src/main/java/com/dailysync/`。
**新增 21 个文件，修改 1 个文件**（`WebMvcConfig`）。包结构变化：

```
com.dailysync
├── common/     + HashUtil
├── auth/       + SyncContext / SyncTokenInterceptor
├── entity/     + Vault / DailyRecord / SyncToken
├── mapper/     + VaultMapper / DailyRecordMapper / SyncTokenMapper
├── dto/        + CreateVaultRequest / VaultResponse / CreateSyncTokenRequest /
│                SyncTokenCreatedResponse / SyncTokenInfoResponse /
│                SyncPushRequest / SyncPushResponse / SyncPullResponse
├── service/    + VaultService / SyncService
└── controller/ + VaultController / SyncController
```

> **风格说明**：M2 的 DTO 全部用 Java `record`（纯数据载体，比 Lombok 更省事，校验注解
> 直接标在组件上）。实体类仍用 Lombok `@Data`（MyBatis-Plus 需要可变对象）。与 M1 混用
> 是有意的，不冲突。

### 3.1 common/HashUtil.java

M1 里 `AuthService` 私有了一份 `sha256Hex`，M2 的令牌与内容哈希也要用，抽成公共工具
（M1 的 `AuthService` 不必回头改，能跑就不动）：

```java
package com.dailysync.common;

import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.util.HexFormat;

public final class HashUtil {

    private HashUtil() {}

    public static String sha256Hex(String input) {
        try {
            byte[] digest = MessageDigest.getInstance("SHA-256")
                    .digest(input.getBytes(StandardCharsets.UTF_8));
            return HexFormat.of().formatHex(digest);
        } catch (Exception e) {
            throw new IllegalStateException("SHA-256 不可用", e);
        }
    }
}
```

### 3.2 auth/SyncContext.java + auth/SyncTokenInterceptor.java

`SyncContext` 与 M1 的 `UserContext` 同款，放当前同步请求的仓库信息：

```java
package com.dailysync.auth;

public final class SyncContext {
    private static final ThreadLocal<Long> USER_ID = new ThreadLocal<>();
    private static final ThreadLocal<Long> VAULT_ID = new ThreadLocal<>();

    private SyncContext() {}

    public static void set(Long userId, Long vaultId) {
        USER_ID.set(userId);
        VAULT_ID.set(vaultId);
    }

    public static Long userId() {
        return USER_ID.get();
    }

    public static Long vaultId() {
        return VAULT_ID.get();
    }

    /** 请求结束后必须清理，防止线程池复用导致串号 */
    public static void clear() {
        USER_ID.remove();
        VAULT_ID.remove();
    }
}
```

`SyncTokenInterceptor`：校验 `X-Sync-Token`，顺带检查所属仓库与账号状态，成功后更新
`last_used_at`。三次查询（令牌→仓库→账号）在个人规模下无所谓，正确性优先：

```java
package com.dailysync.auth;

import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.dailysync.common.ApiResponse;
import com.dailysync.common.HashUtil;
import com.dailysync.entity.SyncToken;
import com.dailysync.entity.User;
import com.dailysync.entity.Vault;
import com.dailysync.mapper.SyncTokenMapper;
import com.dailysync.mapper.UserMapper;
import com.dailysync.mapper.VaultMapper;
import com.fasterxml.jackson.databind.ObjectMapper;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;
import org.springframework.web.method.HandlerMethod;
import org.springframework.web.servlet.HandlerInterceptor;

import java.nio.charset.StandardCharsets;
import java.time.LocalDateTime;

@Component
@RequiredArgsConstructor
public class SyncTokenInterceptor implements HandlerInterceptor {

    private final SyncTokenMapper syncTokenMapper;
    private final VaultMapper vaultMapper;
    private final UserMapper userMapper;
    private final ObjectMapper objectMapper;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler)
            throws Exception {
        if (!(handler instanceof HandlerMethod)) {
            return true;
        }
        String token = request.getHeader("X-Sync-Token");
        // 各种失败一律同一句话，不给探测者区分"令牌不存在/已撤销"的机会
        if (token == null || token.isBlank()) {
            return reject(response);
        }
        SyncToken stored = syncTokenMapper.selectOne(Wrappers.<SyncToken>lambdaQuery()
                .eq(SyncToken::getTokenHash, HashUtil.sha256Hex(token)));
        if (stored == null || stored.getStatus() != 1) {
            return reject(response);
        }
        Vault vault = vaultMapper.selectById(stored.getVaultId());
        if (vault == null) {
            return reject(response);
        }
        User user = userMapper.selectById(vault.getUserId());
        if (user == null || user.getStatus() != 1) {
            return reject(response);
        }
        SyncContext.set(user.getId(), vault.getId());
        syncTokenMapper.update(null, Wrappers.<SyncToken>lambdaUpdate()
                .eq(SyncToken::getId, stored.getId())
                .set(SyncToken::getLastUsedAt, LocalDateTime.now()));
        return true;
    }

    private boolean reject(HttpServletResponse response) throws Exception {
        response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
        response.setContentType("application/json;charset=UTF-8");
        byte[] body = objectMapper.writeValueAsBytes(ApiResponse.error(401, "同步令牌无效或已撤销"));
        response.getOutputStream().write(body);
        response.setContentLength(body.length);
        response.setCharacterEncoding(StandardCharsets.UTF_8.name());
        return false;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response,
                                Object handler, Exception ex) {
        SyncContext.clear();
    }
}
```

### 3.3 实体（3 个）

`entity/Vault.java`：

```java
package com.dailysync.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@TableName("vaults")
public class Vault {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long userId;
    private String name;
    private Long version;
    private LocalDateTime createdAt;
}
```

`entity/DailyRecord.java`：

```java
package com.dailysync.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDate;
import java.time.LocalDateTime;

@Data
@TableName("daily_records")
public class DailyRecord {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long vaultId;
    private String path;
    private String content;
    private String contentHash;
    private Integer deleted;
    private Long version;
    private LocalDate recordDate;
    private LocalDateTime updatedAt;
    private LocalDateTime createdAt;
}
```

`entity/SyncToken.java`：

```java
package com.dailysync.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@TableName("sync_tokens")
public class SyncToken {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long vaultId;
    private String name;
    private String tokenHash;
    private Integer status;
    private LocalDateTime lastUsedAt;
    private LocalDateTime createdAt;
}
```

### 3.4 Mapper（3 个，照旧是空接口）

`mapper/VaultMapper.java`：

```java
package com.dailysync.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dailysync.entity.Vault;

public interface VaultMapper extends BaseMapper<Vault> {
}
```

`mapper/DailyRecordMapper.java`：

```java
package com.dailysync.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dailysync.entity.DailyRecord;

public interface DailyRecordMapper extends BaseMapper<DailyRecord> {
}
```

`mapper/SyncTokenMapper.java`：

```java
package com.dailysync.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dailysync.entity.SyncToken;

public interface SyncTokenMapper extends BaseMapper<SyncToken> {
}
```

### 3.5 DTO（8 个）

`dto/CreateVaultRequest.java`：

```java
package com.dailysync.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;

public record CreateVaultRequest(
        @NotBlank @Size(max = 64, message = "仓库名长度需要 1-64") String name) {
}
```

`dto/VaultResponse.java`：

```java
package com.dailysync.dto;

import java.time.LocalDateTime;

public record VaultResponse(Long id, String name, long version, LocalDateTime createdAt) {
}
```

`dto/CreateSyncTokenRequest.java`：

```java
package com.dailysync.dto;

import jakarta.validation.constraints.Size;

public record CreateSyncTokenRequest(@Size(max = 64, message = "备注名最长 64 字符") String name) {
}
```

`dto/SyncTokenCreatedResponse.java` —— 明文 token 只在这里出现一次：

```java
package com.dailysync.dto;

import java.time.LocalDateTime;

public record SyncTokenCreatedResponse(Long id, String name, String token, LocalDateTime createdAt) {
}
```

`dto/SyncTokenInfoResponse.java`：

```java
package com.dailysync.dto;

import java.time.LocalDateTime;

public record SyncTokenInfoResponse(Long id, String name, int status,
                                    LocalDateTime lastUsedAt, LocalDateTime createdAt) {
}
```

`dto/SyncPushRequest.java` —— 内嵌条目，`deleted=true` 时 content 可省：

```java
package com.dailysync.dto;

import jakarta.validation.Valid;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.Size;

import java.util.List;

public record SyncPushRequest(
        @NotEmpty @Size(max = 200, message = "单次最多推送 200 条") List<@Valid Item> items) {

    public record Item(
            @NotBlank @Size(max = 255) String path,
            @Size(max = 1_048_576, message = "单条内容超过 1MB 上限") String content,
            boolean deleted) {
    }
}
```

`dto/SyncPushResponse.java` —— `status`：`updated`（实际变更）/ `unchanged`（幂等 no-op）：

```java
package com.dailysync.dto;

import java.util.List;

public record SyncPushResponse(long vaultVersion, List<ItemResult> results) {

    public record ItemResult(String path, String status) {
    }
}
```

`dto/SyncPullResponse.java`：

```java
package com.dailysync.dto;

import java.time.LocalDateTime;
import java.util.List;

public record SyncPullResponse(long vaultVersion, boolean hasMore, List<Record> records) {

    public record Record(String path, String content, boolean deleted, long version,
                         LocalDateTime updatedAt) {
    }
}
```

### 3.6 service/VaultService.java

```java
package com.dailysync.service;

import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.dailysync.common.BizException;
import com.dailysync.common.HashUtil;
import com.dailysync.dto.CreateSyncTokenRequest;
import com.dailysync.dto.CreateVaultRequest;
import com.dailysync.dto.SyncTokenCreatedResponse;
import com.dailysync.dto.SyncTokenInfoResponse;
import com.dailysync.dto.VaultResponse;
import com.dailysync.entity.SyncToken;
import com.dailysync.entity.Vault;
import com.dailysync.mapper.SyncTokenMapper;
import com.dailysync.mapper.VaultMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.security.SecureRandom;
import java.time.LocalDateTime;
import java.util.HexFormat;
import java.util.List;

@Service
@RequiredArgsConstructor
public class VaultService {

    private static final SecureRandom RANDOM = new SecureRandom();

    private final VaultMapper vaultMapper;
    private final SyncTokenMapper syncTokenMapper;

    @Transactional
    public VaultResponse create(Long userId, CreateVaultRequest req) {
        Long count = vaultMapper.selectCount(Wrappers.<Vault>lambdaQuery()
                .eq(Vault::getUserId, userId).eq(Vault::getName, req.name()));
        if (count != null && count > 0) {
            throw new BizException(HttpStatus.CONFLICT, "同名仓库已存在");
        }
        Vault vault = new Vault();
        vault.setUserId(userId);
        vault.setName(req.name());
        vault.setVersion(0L);
        vault.setCreatedAt(LocalDateTime.now());
        vaultMapper.insert(vault);
        return toResponse(vault);
    }

    public List<VaultResponse> list(Long userId) {
        return vaultMapper.selectList(Wrappers.<Vault>lambdaQuery()
                        .eq(Vault::getUserId, userId).orderByAsc(Vault::getId)).stream()
                .map(this::toResponse)
                .toList();
    }

    @Transactional
    public SyncTokenCreatedResponse issueToken(Long userId, Long vaultId, CreateSyncTokenRequest req) {
        Vault vault = ownedVault(userId, vaultId);

        byte[] bytes = new byte[32];
        RANDOM.nextBytes(bytes);
        String token = "dst_" + HexFormat.of().formatHex(bytes);

        SyncToken entity = new SyncToken();
        entity.setVaultId(vault.getId());
        entity.setName(req.name() == null ? "" : req.name().trim());
        entity.setTokenHash(HashUtil.sha256Hex(token));
        entity.setStatus(1);
        entity.setCreatedAt(LocalDateTime.now());
        syncTokenMapper.insert(entity);
        // 明文 token 只出现在这一次响应里，之后库里只有哈希
        return new SyncTokenCreatedResponse(entity.getId(), entity.getName(), token, entity.getCreatedAt());
    }

    public List<SyncTokenInfoResponse> listTokens(Long userId, Long vaultId) {
        ownedVault(userId, vaultId);
        return syncTokenMapper.selectList(Wrappers.<SyncToken>lambdaQuery()
                        .eq(SyncToken::getVaultId, vaultId).orderByAsc(SyncToken::getId)).stream()
                .map(t -> new SyncTokenInfoResponse(t.getId(), t.getName(), t.getStatus(),
                        t.getLastUsedAt(), t.getCreatedAt()))
                .toList();
    }

    @Transactional
    public void revokeToken(Long userId, Long vaultId, Long tokenId) {
        ownedVault(userId, vaultId);
        SyncToken token = syncTokenMapper.selectById(tokenId);
        if (token == null || !token.getVaultId().equals(vaultId)) {
            throw new BizException(HttpStatus.NOT_FOUND, "令牌不存在");
        }
        token.setStatus(0);
        syncTokenMapper.updateById(token);
    }

    /** 取属于当前用户的仓库；不存在或不是本人的统一 404，不暴露仓库是否存在 */
    private Vault ownedVault(Long userId, Long vaultId) {
        Vault vault = vaultMapper.selectById(vaultId);
        if (vault == null || !vault.getUserId().equals(userId)) {
            throw new BizException(HttpStatus.NOT_FOUND, "仓库不存在");
        }
        return vault;
    }

    private VaultResponse toResponse(Vault vault) {
        return new VaultResponse(vault.getId(), vault.getName(), vault.getVersion(), vault.getCreatedAt());
    }
}
```

### 3.7 service/SyncService.java（核心）

```java
package com.dailysync.service;

import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.dailysync.common.BizException;
import com.dailysync.common.HashUtil;
import com.dailysync.dto.SyncPullResponse;
import com.dailysync.dto.SyncPushRequest;
import com.dailysync.dto.SyncPushResponse;
import com.dailysync.entity.DailyRecord;
import com.dailysync.entity.Vault;
import com.dailysync.mapper.DailyRecordMapper;
import com.dailysync.mapper.VaultMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeParseException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

@Service
@RequiredArgsConstructor
public class SyncService {

    private final VaultMapper vaultMapper;
    private final DailyRecordMapper recordMapper;

    @Transactional
    public SyncPushResponse push(Long vaultId, SyncPushRequest req) {
        // 先整体校验再动手：任何一条不合法，整批拒绝，不产生半截变更
        Set<String> seenPaths = new HashSet<>();
        for (SyncPushRequest.Item item : req.items()) {
            validatePath(item.path());
            if (!item.deleted() && item.content() == null) {
                throw new BizException(HttpStatus.BAD_REQUEST, "path: " + item.path() + " 缺少 content");
            }
            if (!seenPaths.add(item.path())) {
                throw new BizException(HttpStatus.BAD_REQUEST, "path: " + item.path() + " 在一次推送中重复出现");
            }
        }

        // 第一遍：读现状，判定每条是否需要变更；同一路径只判一次（上面已拒绝重复）
        int n = req.items().size();
        DailyRecord[] existings = new DailyRecord[n];
        boolean[] changed = new boolean[n];
        for (int i = 0; i < n; i++) {
            SyncPushRequest.Item item = req.items().get(i);
            DailyRecord existing = recordMapper.selectOne(Wrappers.<DailyRecord>lambdaQuery()
                    .eq(DailyRecord::getVaultId, vaultId)
                    .eq(DailyRecord::getPath, item.path()));
            existings[i] = existing;
            if (item.deleted()) {
                changed[i] = existing != null && existing.getDeleted() == 0;
            } else {
                changed[i] = existing == null || existing.getDeleted() == 1
                        || !HashUtil.sha256Hex(item.content()).equals(existing.getContentHash());
            }
        }

        // 幂等核心：整批都是 no-op 时不递增版本号，其他设备不会被无意义唤醒
        Vault vault = vaultMapper.selectById(vaultId);
        long version = vault.getVersion();
        boolean anyChange = false;
        for (boolean b : changed) {
            anyChange |= b;
        }
        if (anyChange) {
            // 原子递增仓库版本号；InnoDB 行锁保证并发推送各自拿到不同的版本号
            vaultMapper.update(null, Wrappers.<Vault>lambdaUpdate()
                    .eq(Vault::getId, vaultId)
                    .setSql("version = version + 1"));
            version = vaultMapper.selectById(vaultId).getVersion();
            for (int i = 0; i < n; i++) {
                if (changed[i]) {
                    applyChange(vaultId, req.items().get(i), existings[i], version);
                }
            }
        }

        List<SyncPushResponse.ItemResult> results = new ArrayList<>(n);
        for (int i = 0; i < n; i++) {
            results.add(new SyncPushResponse.ItemResult(req.items().get(i).path(),
                    changed[i] ? "updated" : "unchanged"));
        }
        return new SyncPushResponse(version, results);
    }

    /** 落库单条变更（调用前已确认该条需要变更） */
    private void applyChange(Long vaultId, SyncPushRequest.Item item, DailyRecord existing, long version) {
        if (item.deleted()) {
            // 墓碑：内容清空省空间
            existing.setDeleted(1);
            existing.setContent("");
            existing.setContentHash("");
            existing.setVersion(version);
            existing.setRecordDate(null);
            existing.setUpdatedAt(LocalDateTime.now());
            recordMapper.updateById(existing);
            return;
        }
        String hash = HashUtil.sha256Hex(item.content());
        if (existing == null) {
            DailyRecord record = new DailyRecord();
            record.setVaultId(vaultId);
            record.setPath(item.path());
            record.setContent(item.content());
            record.setContentHash(hash);
            record.setDeleted(0);
            record.setVersion(version);
            record.setRecordDate(deriveRecordDate(item.path()));
            record.setUpdatedAt(LocalDateTime.now());
            record.setCreatedAt(LocalDateTime.now());
            recordMapper.insert(record);
        } else {
            existing.setContent(item.content());
            existing.setContentHash(hash);
            existing.setDeleted(0);
            existing.setVersion(version);
            existing.setRecordDate(deriveRecordDate(item.path()));
            existing.setUpdatedAt(LocalDateTime.now());
            recordMapper.updateById(existing);
        }
    }

    public SyncPullResponse pull(Long vaultId, long since, int limit) {
        Vault vault = vaultMapper.selectById(vaultId);
        List<DailyRecord> rows = recordMapper.selectList(Wrappers.<DailyRecord>lambdaQuery()
                .eq(DailyRecord::getVaultId, vaultId)
                .gt(DailyRecord::getVersion, since)
                .orderByAsc(DailyRecord::getVersion)
                .orderByAsc(DailyRecord::getId)
                .last("LIMIT " + limit));
        List<SyncPullResponse.Record> records = rows.stream()
                .map(r -> new SyncPullResponse.Record(r.getPath(), r.getContent(),
                        r.getDeleted() == 1, r.getVersion(), r.getUpdatedAt()))
                .toList();
        return new SyncPullResponse(vault.getVersion(), records.size() == limit, records);
    }

    private void validatePath(String path) {
        String problem = null;
        if (path.startsWith("/") || path.contains("\\")) {
            problem = "必须是库内相对路径";
        } else {
            for (String segment : path.split("/")) {
                if (segment.equals("..") || segment.equals(".") || segment.isEmpty()) {
                    problem = "路径段不能是 . / .. / 空";
                    break;
                }
            }
        }
        if (problem == null && !path.endsWith(".md")) {
            problem = "M2 只同步 .md 文件";
        }
        if (problem != null) {
            throw new BizException(HttpStatus.BAD_REQUEST, "path: " + path + " " + problem);
        }
    }

    /** 从文件名首段解析记录日期（仅 YYYY-MM-DD 认可；周记 2026-W37 等返回 null），供 M4 前端按日查询 */
    private LocalDate deriveRecordDate(String path) {
        String filename = path.substring(path.lastIndexOf('/') + 1);
        // 先剥 .md 后缀：无空格的文件名（2026-09-09.md）首段会带着后缀导致匹配不上
        if (filename.endsWith(".md")) {
            filename = filename.substring(0, filename.length() - 3);
        }
        String firstToken = filename.split(" ")[0];
        if (!firstToken.matches("\\d{4}-\\d{2}-\\d{2}")) {
            return null;
        }
        try {
            return LocalDate.parse(firstToken);
        } catch (DateTimeParseException e) {
            return null;
        }
    }
}
```

> **关于 pull 的一致性**：pull 不加事务，`selectById(vault)` 与记录查询是两个自动提交的读。
> 极端情况下两次读之间恰好有推送提交，游标规则（1.4 节）保证客户端最多重复收到、
> 不会漏收：`hasMore=false` 时用 `vaultVersion` 做下次游标，若本页已包含更新的版本号，
> 客户端应取 `max(vaultVersion, 本页最大 version)`。

### 3.8 controller/VaultController.java + controller/SyncController.java

`controller/VaultController.java`（JWT 保护，路径在 `/api/v1/**` 拦截范围内，无需额外配置）：

```java
package com.dailysync.controller;

import com.dailysync.auth.UserContext;
import com.dailysync.common.ApiResponse;
import com.dailysync.dto.CreateSyncTokenRequest;
import com.dailysync.dto.CreateVaultRequest;
import com.dailysync.dto.SyncTokenCreatedResponse;
import com.dailysync.dto.SyncTokenInfoResponse;
import com.dailysync.dto.VaultResponse;
import com.dailysync.service.VaultService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/v1/vaults")
@RequiredArgsConstructor
public class VaultController {

    private final VaultService vaultService;

    @PostMapping
    public ApiResponse<VaultResponse> create(@Valid @RequestBody CreateVaultRequest req) {
        return ApiResponse.ok(vaultService.create(UserContext.userId(), req));
    }

    @GetMapping
    public ApiResponse<List<VaultResponse>> list() {
        return ApiResponse.ok(vaultService.list(UserContext.userId()));
    }

    @PostMapping("/{id}/tokens")
    public ApiResponse<SyncTokenCreatedResponse> issueToken(@PathVariable Long id,
                                                            @Valid @RequestBody CreateSyncTokenRequest req) {
        return ApiResponse.ok(vaultService.issueToken(UserContext.userId(), id, req));
    }

    @GetMapping("/{id}/tokens")
    public ApiResponse<List<SyncTokenInfoResponse>> listTokens(@PathVariable Long id) {
        return ApiResponse.ok(vaultService.listTokens(UserContext.userId(), id));
    }

    @DeleteMapping("/{id}/tokens/{tokenId}")
    public ApiResponse<Void> revokeToken(@PathVariable Long id, @PathVariable Long tokenId) {
        vaultService.revokeToken(UserContext.userId(), id, tokenId);
        return ApiResponse.ok(null);
    }
}
```

`controller/SyncController.java`（同步令牌保护，见 3.9 的拦截器配置）：

```java
package com.dailysync.controller;

import com.dailysync.auth.SyncContext;
import com.dailysync.common.ApiResponse;
import com.dailysync.dto.SyncPullResponse;
import com.dailysync.dto.SyncPushRequest;
import com.dailysync.dto.SyncPushResponse;
import com.dailysync.service.SyncService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/v1/sync")
@RequiredArgsConstructor
public class SyncController {

    private final SyncService syncService;

    @PostMapping
    public ApiResponse<SyncPushResponse> push(@Valid @RequestBody SyncPushRequest req) {
        return ApiResponse.ok(syncService.push(SyncContext.vaultId(), req));
    }

    @GetMapping
    public ApiResponse<SyncPullResponse> pull(@RequestParam(name = "since", defaultValue = "0") long since,
                                              @RequestParam(name = "limit", defaultValue = "200") int limit) {
        int clamped = Math.min(Math.max(limit, 1), 500);
        return ApiResponse.ok(syncService.pull(SyncContext.vaultId(), since, clamped));
    }
}
```

### 3.9 config/WebMvcConfig.java（本期唯一修改的文件）

JWT 拦截器把 `/api/v1/sync` 排除（它用同步令牌鉴权），再注册同步令牌拦截器接管它：

```java
package com.dailysync.config;

import com.dailysync.auth.AuthInterceptor;
import com.dailysync.auth.SyncTokenInterceptor;
import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
@RequiredArgsConstructor
public class WebMvcConfig implements WebMvcConfigurer {

    private final AuthInterceptor authInterceptor;
    private final SyncTokenInterceptor syncTokenInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(authInterceptor)
                .addPathPatterns("/api/v1/**")
                .excludePathPatterns("/api/v1/auth/**")
                // 同步接口走 X-Sync-Token 鉴权，不走 JWT
                .excludePathPatterns("/api/v1/sync");
        registry.addInterceptor(syncTokenInterceptor)
                .addPathPatterns("/api/v1/sync");
    }
}
```

`application.yml` 本期**无需任何改动**（数据源、JWT 密钥沿用 M1）。

---

## 4. 运行

```bash
cd /h/develop/project/daily-sync/backend
mvn spring-boot:run
```

看到 `Started DailySyncApplication` 即可（M2 新增了 3 个 Mapper，`@MapperScan` 已覆盖，无需改动）。

---

## 5. 验证（Git Bash 里逐步执行）

**① 登录拿 accessToken**（M1 已注册的用户 `shuiyi`）：

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"shuiyi","password":"yourPassword123"}'
# 复制 data.accessToken，下文记作 $AT
```

**② 创建仓库（第一次，应成功）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/vaults \
  -H "Authorization: Bearer $AT" -H "Content-Type: application/json" \
  -d '{"name":"main"}'
# 预期：{"code":0,...,"data":{"id":1,"name":"main","version":0,"createdAt":"..."}}
```

**③ 同名仓库（应 409）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/vaults \
  -H "Authorization: Bearer $AT" -H "Content-Type: application/json" \
  -d '{"name":"main"}'
# 预期：HTTP 409 {"code":409,"message":"同名仓库已存在",...}
```

**④ 签发同步令牌（明文只出现这一次，复制保存）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/vaults/1/tokens \
  -H "Authorization: Bearer $AT" -H "Content-Type: application/json" \
  -d '{"name":"desktop"}'
# 预期：data.token 形如 dst_ + 64 位十六进制；记作 $ST
```

**⑤ 令牌列表（不含明文）**：

```bash
curl -s http://localhost:8080/api/v1/vaults/1/tokens -H "Authorization: Bearer $AT"
# 预期：列表里有 desktop，无 token 字段，status=1
```

**⑥ 推送两条记录（一篇日记 + 一篇带名字的日记）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/sync \
  -H "X-Sync-Token: $ST" -H "Content-Type: application/json" \
  -d '{"items":[
        {"path":"diary/2026-09-09.md","content":"# 今日\n- 完成M2文档"},
        {"path":"diary/2026-09-09 晚上.md","content":"# 晚上\n散步"}
      ]}'
# 预期：vaultVersion=1，两条 status=updated
```

**⑦ 原样重推（幂等：应全部 unchanged，版本不推进）**：

```bash
# 与 ⑥ 完全相同的请求再发一次
# 预期：vaultVersion=1（没变！），两条 status=unchanged
```

**⑧ 全量拉取**：

```bash
curl -s "http://localhost:8080/api/v1/sync?since=0" -H "X-Sync-Token: $ST"
# 预期：vaultVersion=1，hasMore=false，两条记录 version=1
```

**⑨ 修改其中一条再推（版本推进到 2）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/sync \
  -H "X-Sync-Token: $ST" -H "Content-Type: application/json" \
  -d '{"items":[{"path":"diary/2026-09-09.md","content":"# 今日\n- 完成M2文档\n- 跑步 5km"}]}'
# 预期：vaultVersion=2，status=updated

curl -s "http://localhost:8080/api/v1/sync?since=1" -H "X-Sync-Token: $ST"
# 预期：只返回被修改的这一条（version=2）——增量拉取生效
```

**⑩ 墓碑删除**：

```bash
curl -s -X POST http://localhost:8080/api/v1/sync \
  -H "X-Sync-Token: $ST" -H "Content-Type: application/json" \
  -d '{"items":[{"path":"diary/2026-09-09 晚上.md","deleted":true}]}'
# 预期：vaultVersion=3，status=updated

curl -s "http://localhost:8080/api/v1/sync?since=2" -H "X-Sync-Token: $ST"
# 预期：该记录 deleted=true（其他设备据此删本地文件）
```

**⑪ 非法路径（应 400）**：

```bash
curl -s -X POST http://localhost:8080/api/v1/sync \
  -H "X-Sync-Token: $ST" -H "Content-Type: application/json" \
  -d '{"items":[{"path":"../secret.md","content":"x"}]}'
# 预期：HTTP 400，message 含 "路径段不能是 . / .. / 空"
```

**⑫ 错误令牌 / 撤销后的令牌（应 401）**：

```bash
curl -s "http://localhost:8080/api/v1/sync?since=0" -H "X-Sync-Token: dst_deadbeef"
# 预期：HTTP 401 {"code":401,"message":"同步令牌无效或已撤销",...}

# 撤销 ⑤ 里看到的令牌 id（假设是 1），再用真令牌推送：
curl -s -X DELETE http://localhost:8080/api/v1/vaults/1/tokens/1 -H "Authorization: Bearer $AT"
curl -s "http://localhost:8080/api/v1/sync?since=0" -H "X-Sync-Token: $ST"
# 预期：HTTP 401（撤销立即生效）
```

**⑬ 数据库检查**：

```bash
mysql -h120.26.58.22 -u daily_sync -p daily_sync
```

```sql
SELECT id, name, version FROM vaults;
SELECT vault_id, path, LEFT(content_hash, 12) AS hash, deleted, version, record_date
  FROM daily_records ORDER BY id;
SELECT id, vault_id, name, status, last_used_at FROM sync_tokens;
```

预期要点：
- `daily_records` 共 2 行（墓碑不删行）：`2026-09-09.md` 的 `record_date=2026-09-09`、deleted=0；
  `2026-09-09 晚上.md` deleted=1、content 为空串
- `sync_tokens.last_used_at` 已更新（⑫ 之前的调用都会刷新它）

**⑭（可选）仓库隔离**：再注册一个用户、建他自己的仓库、签他自己的令牌，
用他的令牌 `since=0` 拉取 → 空列表。用他的令牌推 `diary/2026-09-09.md`
→ 落在他自己的仓库里，与 shuiyi 的记录互不影响。

---

## 6. 环境变量管理准备

M1 的 `application.yml` 已用 `${ENV:默认值}` 形式，代码零改动。本期把"正式密钥"准备好：

**① 生成正式 JWT 密钥**（替换 dev 默认值）：

```bash
openssl rand -hex 32   # 输出 64 位十六进制，>= 32 字节要求满足
```

**② 本地运行方式**（Git Bash，环境变量优先于 yml 默认值）：

```bash
cd /h/develop/project/daily-sync/backend
DB_PASSWORD=DailySync_2026 JWT_SECRET=粘贴上面生成的串 mvn spring-boot:run
```

**③ 约定 `.env` 文件**：仓库根建 `.env.example`（提交，占位值）与 `.env`（本地真实值，
M1 的 `.gitignore` 已忽略 `.env`）：

```bash
# .env.example —— 复制为 .env 并填入真实值；.env 永远不提交
DB_PASSWORD=change-me
JWT_SECRET=change-me-openssl-rand-hex-32
INVITE_CODE=
```

> Spring Boot 原生不读 `.env`（那是 docker-compose / 部署工具的职责），M6 的
> docker-compose `env_file` 会直接吃掉它。现阶段 `.env` 是" single source of truth
> 的备忘"，手动 `source .env && mvn spring-boot:run` 也可（导出为环境变量即可生效）。
> 数据库应用账号密码想换的话，连上 MySQL 执行
> `ALTER USER 'daily_sync'@'%' IDENTIFIED BY '新密码';` 后同步改 `.env`。

---

## 7. 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 推送报 500，日志有 `JSON parse error: Invalid UTF-8 start byte` | **Windows Git Bash 的 curl 把命令行里的中文按 GBK 发出去了**（不是服务端 bug）。验证时把请求体写进 UTF-8 文件，用 `curl --data-binary @body.json` 发送；插件端（Electron `requestUrl`）不受影响 |
| 推送报 500，日志有 `DuplicateKeyException` | 两台设备/两次重试在同一瞬间推同一路径，唯一键兜住了竞态；**重试一次**即可（内容相同会走 unchanged）。若频繁出现再考虑串行化，个人场景基本碰不到 |
| 令牌刚签发就 401 | 逐项检查：header 名是 `X-Sync-Token`；带完整 `dst_` 前缀；没有多余引号/空格；令牌未被撤销（`GET /api/v1/vaults/{id}/tokens` 看 status） |
| `/api/v1/sync` 返回 401"未登录或令牌已失效" | JWT 拦截器没排除该路径——`WebMvcConfig` 没改对；两种 401 文案可区分是哪个拦截器在拦你 |
| path 被拒 | 规则见 1.6：不以 `/` 开头、无 `\`、无 `.`/`..` 段、`.md` 结尾、≤255 |
| 拉取一直 hasMore=true | 按 1.4 游标规则继续翻页（since=最后一条 version-1，按 path 去重）；若单仓库记录数超过 500×多轮仍翻不完，检查客户端是否每轮都在重置 since |
| `record_date` 是 NULL | 文件名首段不是 `YYYY-MM-DD`（周记 `2026-W37`、乱起名）——设计如此，不是 bug |
| 时间字段差 8 小时 | 同 M1：JDBC URL 的 `serverTimezone=Asia/Shanghai` 必须保留 |
| 想彻底删除墓碑数据 | M2 不提供接口（防旧设备"诈尸"）；确要清理，SQL 里 `DELETE FROM daily_records WHERE deleted=1 AND vault_id=?`，风险自担 |

---

## 8. 验收清单与下一步

M2 完成标准（全部打勾）：

- [ ] 三张表建成（第 2 节 SQL 幂等，重复执行不报错）
- [ ] 第 5 节 ①~⑬ 全部符合预期（⑭ 可选）
- [ ] **幂等性**：原样重推返回 unchanged 且 `vaultVersion` 不变（⑦）
- [ ] **撤销即时生效**：DELETE 令牌后立刻 401（⑫）
- [ ] **增量正确**：`since=1` 只返回修改过的记录（⑨）
- [ ] `.env.example` 已建、正式 JWT 密钥已生成并按环境变量方式跑通一次
- [ ] 代码已提交：`git add -A && git commit -m "M2: 数据同步（仓库/记录/同步令牌）"`

**M3 预告（插件端接入）**：`quick-daily-note` 增加同步设置区块（服务器地址 + 同步令牌），
启动时 `since=0` 全量拉取、之后增量；监听 `vault.modify`/`delete` 钩子防抖推送；
本地保存 `sync-state.json`（path → 上次同步哈希 + 游标）；冲突按 1.3 节策略落地；
状态栏显示同步状态。服务端无需改动。

**部署路线不变**：M6 统一 docker-compose 上云（120.26.58.22），`.env` 届时正式接管配置注入。
