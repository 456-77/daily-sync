# Daily Sync M3 实施文档 —— 插件端对接（quick-daily-note 云同步）

> 目标：给 `quick-daily-note` 插件（v2.6.0）接上 M2 的同步接口，实现多设备日记同步：
> 设置区块（服务端地址 + `dst_` 令牌）、文件钩子防抖推送、增量拉取、墓碑删除、
> 冲突落地（本地胜）、状态栏指示。**服务端零改动**。
>
> 本期代码全部在 `quick-daily-note/` 内（该目录有独立 git，根仓库已 gitignore）。

---

## 0. 本期范围

**做什么：**

- 新模块 `sync.ts`（约 470 行）：`SyncManager` 类，封装全部同步逻辑
- `main.ts` 接线：设置字段、命令、状态栏、设置页"云同步"区块
- 同步状态存插件本地 `data.json` 的 `sync` 键（**不是**库内文件，见 1.1）
- headless 集成测试 `scripts/run-sync-test.mjs`：stub 掉 obsidian 模块，
  真实 `SyncManager` 逻辑直连本地后端，23 项断言全过

**不做什么（留给后续）：**

- ❌ 附件/图片同步（仍只同步 `.md`）
- ❌ 三路合并 / 冲突对比界面（本期为"本地胜 + Notice 提示"）
- ❌ M4 前端（令牌签发仍需 curl / 接口工具）
- ❌ README 更新（发版时一并做）

---

## 1. 设计

### 1.1 同步状态放哪：本地 data.json 的 `sync` 键

M2 文档预告过 `sync-state.json`，实际落地改为 **`data.json` 的 `sync` 键**，理由：

- 游标和每文件哈希是**每设备独立**的（两台设备游标不同步推进），放库内会被
  本插件自己同步出去，互相覆盖——必须放设备本地；
- 插件本来就有 data.json（`.obsidian/plugins/quick-daily-note/`，Remotely Save 等
  同步工具默认跳过 `.obsidian`），直接复用，不新增文件；
- 与背景设置的存储方式一致（`saveLocalData()` 统一写入）。

`data.json` 结构（v2.6.0 起）：

```json
{
  "bgLocalVersion": 1,
  "bgEnabled": true,
  "...": "其余背景键",
  "sync": {
    "serverUrl": "http://120.26.58.22:8080",
    "token": "dst_xxxx…",
    "enabled": true,
    "scope": "folder",
    "cursor": 12,
    "hashes": { "日记/2026-09-09.md": "sha256-hex…" },
    "lastSyncAt": 1789002215469
  }
}
```

`hashes` 是冲突判定的心脏：**path → 上次同步成功时的内容哈希**。
"本地改过没"= 当前哈希 ≠ `hashes[path]`；"云端改过没"= 拉取内容哈希 ≠ `hashes[path]`。

### 1.2 同步周期

| 触发 | 时机 | 动作 |
| --- | --- | --- |
| 启动同步 | 布局就绪后 5s（`enabled` 且已配置） | 完整周期 |
| 周期同步 | 每 5 分钟 | 完整周期 |
| 防抖推送 | 本地 `modify/create/delete/rename` 后静默 3s | 仅推送 |
| 手动 | 命令「立即云同步」/ 状态栏点击 / 设置页按钮 | 完整周期 |
| 配置变更 | 设置页改地址/令牌/开关后 2.5s | 完整周期（验证连通性） |

完整周期 = **扫描本地 → 拉取应用 → 推送队列**，全程持锁（`syncing` 标志），
周期内再触发会合并为一轮补跑（`rerun`）。

### 1.3 推送（防抖 + 批量）

- vault 事件进 `dirty: Map<path, "mod" | "del">`，3s 静默后发送；
- 发送时**实时读文件内容**（防抖窗口内的多次修改只有最后一次生效）；
  文件已不在 → 转墓碑；
- 当前哈希 === `hashes[path]` 的条目跳过（省流量，服务端反正会答 unchanged）；
- 重命名 = 旧路径墓碑 + 新路径内容（旧路径即使移出了同步范围也发墓碑，
  服务端对未知路径的墓碑是 no-op，安全）；
- 单批 ≤200 条（服务端上限），超出自动分批；
- >1MB 的文件跳过并 Notice（服务端上限 1MB）。

### 1.4 拉取与应用

按 M2 1.4 节分页规则翻页（`limit=500`；`hasMore=true` 时 `since = 末条 version - 1`，
按 path 去重取最新），然后逐条应用：

| 云端 | 本地 | 动作 |
| --- | --- | --- |
| 有内容 | 无文件 | 创建（含建父目录） |
| 有内容 | 一致 | 只更新 hashes |
| 有内容 | 本地未改（哈希=hashes[path]） | 云端覆盖 |
| 有内容 | 本地改过 | **冲突：本地胜，标记重推** |
| 墓碑 | 无文件 | 清 hashes |
| 墓碑 | 本地未改 | 移入回收站（`.trash`） |
| 墓碑 | 本地改过 | **冲突：保留本地并重推（复活）** |
| 上次同步的旧内容 | 无文件（且 hashes 记录===云端哈希） | **本地离线删除 → 补推墓碑** |
| 新内容 | 无文件（hashes 记录≠云端哈希） | 云端胜，重建文件 |

应用期间 `applyingRemote` 计数器 >0，自己触发的 vault 事件不会回流进推送队列。

**自己推送的回声**：本设备推送会推高仓库版本，下次拉取会收到自己的记录——
应用时"本地一致"分支直接吸收，无副作用，只是多一次下载（个人日记规模无所谓；
不采取"推送后跳游标"是因为并发窗口下会漏掉其他设备的变更）。

### 1.5 冲突策略（M2 1.3 节的落地）

双边修改 → **本地胜**：保留本地文件内容，同一周期内重新推送覆盖云端，
Notice 列出冲突文件（最多列 5 个）。被覆盖一端下次拉取时本地未改，
直接被云端（胜者）覆盖——即最终一致性靠"后推者胜"传播。

### 1.6 同步范围

- `scope=folder`（默认）：只推 `settings.folder`（日记存放位置）内的 `.md`；
- `scope=vault`：推全库 `.md`；
- 任何范围都排除点开头路径段（`.obsidian/`、`.trash/`、`.git/`…）；
- **拉取不受范围限制**：云端已有的记录一律落地本地（否则状态机会对不上）。

### 1.7 新设备首次接入

`scanLocalFiles()` 在每个完整周期开头把"本地存在但 `hashes` 不一致"的文件全部入队：
新设备（或老设备装了已有日记的库）第一次同步即完成**全量上传**，
无需任何"初始化"特例。反之_hashes 里有、本地没有、云端也没动的 → 补推墓碑（离线删除）。

---

## 2. 代码变更清单

### 2.1 新增 `sync.ts`

```
导出：
  SyncDeviceState / DEFAULT_SYNC_STATE / normalizeSyncState
  SyncStatusKind = "off" | "syncing" | "ok" | "error"
  SyncHost { getFolder(); persist(); onStatus(kind, detail?) }
  SyncManager:
    start()                    注册 vault 钩子 + 5min 定时器
    beginStartupSync()         布局就绪后延迟首同步
    onConfigChanged()          设置变更后防抖触发验证同步
    resetCursorAndSync()       游标清零全量重拉（自愈入口）
    syncNow(trigger)           完整周期（互斥，重入合并）
    destroy()                  清理 + 尽力冲刷未发变更
  sha256Hex(content)           与后端 HashUtil.utf8-bytes 一致
内部：
  dirty 队列 / applyingRemote 计数 / syncing 锁 / 错误 Notice 去重
  inScope(path) / scanLocalFiles() / doFlush() / pullAndApply() / applyRecord()
  api(method, since?, body?)   requestUrl 封装（throw:false，手动判 status）
```

关键点：

- HTTP 用 obsidian 的 `requestUrl`（Electron 通道，无 CORS 问题，移动端也可用）；
  地址没写协议时自动补 `http://`；
- 401 单独识别为令牌问题，Notice 引导去设置页更新，且**同一错误只提示一次**
  （成功后复位），周期重试不刷屏；
- `crypto.subtle.digest` 算 SHA-256（Obsidian 桌面/移动端均可用）。

### 2.2 `main.ts` 接线（改动集中，共 7 处）

1. import sync 模块；
2. 插件字段：`syncState` / `localData` / `syncManager` / `syncStatusEl`；
3. `onload`：两个命令（`sync-now` / `sync-full-repull`）、状态栏元素、
   创建并 `start()` SyncManager、`onLayoutReady` 挂首同步；
4. `onunload`：`syncManager.destroy()`；
5. `loadSettings`：读取 `local.sync` → `normalizeSyncState`；`localData` 保存原始内容；
6. `saveLocalBackgroundSettings` 更名 `saveLocalData`：data.json 写入
   `{...localData, bgLocalVersion:1, sync: syncState, ...背景键}`（背景逻辑不变，
   只是合并保留 sync 键）；
7. 设置页新增「云同步（daily-sync 服务）」区块（使用说明之前）：开关、地址、
   令牌、范围下拉、手动同步按钮 + 动态状态行（上次同步时间/游标/文件数）。

### 2.3 版本

`manifest.json` / `package.json` → **2.6.0**，`versions.json` 补 `"2.6.0": "1.8.7"`。

### 2.4 测试资产（不随 npm 包发布）

- `scripts/obsidian-stub.cjs`：obsidian 模块最小桩（Notice 记录、TFile、requestUrl→fetch）；
- `scripts/run-sync-test.mjs`：esbuild 把 `sync.ts` 打包为 CJS、stub 装进
  `.test-build/node_modules/obsidian`（与 bundle 共享类身份，`instanceof TFile` 才成立），
  然后驱动真 SyncManager 对本地后端跑 8 个场景；
- `.gitignore` 增加 `.test-build/`。

---

## 3. 构建 / 部署 / 验证

### 3.1 构建

```bash
cd /h/develop/project/daily-sync/quick-daily-note
npm run build        # tsc -noEmit + esbuild production → main.js
```

### 3.2 headless 集成测试（真实逻辑 × 真实后端）

```bash
# 前置：后端在 localhost:8080 运行（.env 方式启动，见 M2 文档第 6 节）
node scripts/run-sync-test.mjs
```

场景与断言（23 项，全部通过，2026-09-10）：

| 场景 | 验证点 |
| --- | --- |
| 1 首次同步上传 | 本地既有日记全量上传；范围外文件（`其他/readme.md`）不上传；重复同步 no-op；游标行为 |
| 2 修改防抖推送 | modify 事件 → 3s 后服务端收到新内容 |
| 3 设备 B 全量拉取 | 空库设备拉到全部文件 |
| 4 双边修改冲突 | A、B 同文件各自修改 → 后推的 B 先上云，A 拉取判冲突 → 保留 A 本地并重推 → B 最终被 A 覆盖；冲突 Notice |
| 5 删除墓碑 | A 删文件 → 服务端墓碑 → B 拉取后文件进回收站 |
| 6 离线删除 | destroy 后磁盘直删 → 重启（复用持久化 state）→ 拉取分支补推墓碑 |
| 7 分页拉取 | 521 条活记录 + 墓碑，新设备一次同步拉全（500/页翻页） |
| 8 无效令牌 | 401 → error 状态 + 明确 Notice，一次不刷屏 |

> 测试会在服务端留下 `plugtest-*` 仓库与数据（含 >500 条 bulk 记录），
> 属预期测试痕迹；介意可 SQL 清理：
> `DELETE FROM daily_records WHERE vault_id IN (SELECT id FROM vaults WHERE name LIKE 'plugtest-%');`
> `DELETE FROM sync_tokens WHERE vault_id IN (...); DELETE FROM vaults WHERE name LIKE 'plugtest-%';`

### 3.3 部署到测试库（手动验证用）

```bash
cp main.js manifest.json "/h/develop/shuiyi_code/quick daily note/.obsidian/plugins/quick-daily-note/"
```

### 3.4 Obsidian 内手动验收清单

前置：为本机测试库签一枚令牌——

```bash
AT=$(curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" \
  -d '{"username":"shuiyi","password":"…"}' | jq -r .data.accessToken)
curl -s -X POST http://localhost:8080/api/v1/vaults -H "Authorization: Bearer $AT" \
  -H "Content-Type: application/json" -d '{"name":"obsidian-test"}'
curl -s -X POST http://localhost:8080/api/v1/vaults/<id>/tokens -H "Authorization: Bearer $AT" \
  -H "Content-Type: application/json" -d '{"name":"我的电脑"}'   # 复制 data.token
```

1. 重载插件（或重启 Obsidian）→ 设置 → Quick Daily Note → 云同步区块；
2. 填地址 `http://localhost:8080`（或服务器 IP）、粘贴 `dst_` 令牌、开开关；
   约 2.5s 后自动首同步，状态栏出现 `✓ 已同步 HH:MM`；
3. 改一篇日记 → ~3s 后状态栏刷新；服务端 `GET /sync?since=0` 可见新内容；
4. 删一篇日记 → 服务端墓碑；如另一台设备（或换个 vault 目录再建一个库）同令牌拉取，
   文件消失（进 `.trash`）；
5. 命令面板：「立即云同步」「重置同步游标并全量拉取」；
6. 故意改错令牌 → 状态栏 `⚠ 同步失败`，Notice 提示令牌问题（只提示一次）。

---

## 4. 常见问题（FAQ）

| 现象 | 原因与处理 |
| --- | --- |
| 两台设备各改同一篇日记 | 设计语义：本地胜 + 后推者胜（见 1.5）。被覆盖端不丢当时正在看的文件，但云端以胜者为准。个人日记场景可接受；需要三方合并属后续里程碑 |
| 新设备一接上就把云端文件"删了" | 早期 bug：`{...DEFAULT_SYNC_STATE}` 浅拷贝导致多实例共享 `hashes`，把"本地没有"误判为离线删除。已用 `normalizeSyncState` 深拷贝修复（headless 测试场景 3/6 回归覆盖）。若再遇到，先怀疑状态污染：命令「重置同步游标并全量拉取」可自愈 |
| 推送/拉取了范围外的文件 | 拉取不受范围限制（1.6）；推送范围看设置。想缩范围：改回"日记文件夹"并把云端多余记录按需清理 |
| 路径大小写 | Windows 库改文件名大小写时，服务端（MySQL ci 排序规则）视为同一路径，本地 Obsidian 视为 rename——钩子会推"旧路径墓碑 + 新路径内容"，实际表现正确；极端大小写仅差的双文件场景避免人为制造 |
| 1MB 以上的 md | 服务端拒绝；插件直接跳过并 Notice。日记不该这么大，多半是误粘贴了数据 |
| 状态栏不显示 | 未配置或未开开关时隐藏（off）；开 `enabled` 后首个周期完成才出现 ✓ |
| 令牌换新后旧令牌还想去同步 | 撤销旧令牌（DELETE 接口）立即 401；插件端改 data.json 里 sync.token 即可 |
| 想彻底重置本设备同步状态 | 关插件 → 删 `data.json` 里 `sync` 键（或整键改 `{"cursor":0,"hashes":{}}`）→ 开插件 → 命令全量重拉 |
| 移动端能用吗 | `isDesktopOnly:false`，requestUrl/crypto.subtle 移动端均可用；但移动端填 `localhost` 地址需换成局域网 IP/内网穿透 |

---

## 5. 验收清单与下一步

M3 完成标准：

- [x] `npm run build` 通过（tsc strict）
- [x] headless 集成测试 23/23（真实后端、真实 SyncManager）
- [x] 已部署 main.js/manifest.json 到测试库 `H:\develop\shuiyi_code\quick daily note`
- [ ] Obsidian 内手动验收（3.4 节，需人工过一遍）
- [ ] 插件仓库提交：`git add -A && git commit -m "M3: 云同步客户端（推送/拉取/墓碑/冲突本地胜）"`

**M4 预告（前端）**：React 前端——登录、仓库管理、令牌签发/撤销界面、按日查询日记
（吃 `record_date`）。届时插件端"获取令牌"不再需要 curl。

**部署路线不变**：M6 docker-compose 上云，`.env` 接管配置。
