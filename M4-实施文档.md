# Daily Sync M4 实施文档 —— Web 前端（登录 / 仓库 / 令牌 / 日记浏览）

> 目标：React 前端 + 配套查询接口。装好插件后"拿令牌"不用再 curl——
> 浏览器登录 → 建仓库 → 点一下签发令牌 → 复制进 Obsidian；顺便提供日历打点式
> 的日记按日浏览（吃 M2 就备好的 record_date）。
>
> 本期改后端 2 个文件、新增 4 个；前端全新目录 `frontend/`（Vite + React 19 + TS）。

---

## 0. 本期范围

**做什么：**

- 后端新增两个查询接口（JWT 保护）：
  - `GET /api/v1/vaults/{id}/records?date=YYYY-MM-DD` —— 某天的全部记录（含内容）
  - `GET /api/v1/vaults/{id}/records/dates?from=&to=` —— 各日记录数（日历打点）
- React 前端 `frontend/`：
  - 登录页（用户名/密码；accessToken 过期自动用 refreshToken 轮换续期）
  - 仓库页（列表 / 新建 / 选中当前仓库）
  - 令牌页（列表含状态与最近使用 / 签发 / 撤销；**明文令牌仅在签发弹窗展示一次**）
  - 日记浏览页（月历打点 → 点日期看当天记录 → Markdown 渲染阅读）

**不做什么（M5/M6）：**

- ❌ 注册界面（邀请码注册仍走 curl / 接口工具，防随手开放注册）
- ❌ 日记在线编辑（只读浏览；编辑回 Obsidian，避免两套写入路径打架）
- ❌ 生产部署整合（M6 把 `frontend/dist` 塞进 Spring Boot static，本期开发态走 vite 代理）

---

## 1. 后端：两个查询接口

### 1.1 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/v1/vaults/{id}/records?date=` | 当天记录（含 content，按 path 升序；一天几篇，不分页） |
| GET | `/api/v1/vaults/{id}/records/dates?from=&to=` | `[{date, count}]` 按日期升序；from/to 缺省最近 180 天 |

规则：墓碑一律排除；`record_date IS NULL`（周记等）不参与；仓库不存在或非本人 404
（复用 `VaultService.ownedVault`，本期从 private 改 public）。

### 1.2 代码

- `dto/DailyRecordResponse.java`、`dto/RecordDateCountResponse.java`（record）
- `service/RecordQueryService.java`：日历计数走 SQL `GROUP BY record_date`
  （`selectMaps`，不拉明细）；GROUP BY 结果的日期列类型随驱动可能是
  `java.sql.Date`/`LocalDate`/`String`，`toDate()` 统一转换
- `controller/RecordController.java`
- 改 `VaultService.ownedVault` 为 public（归属校验供跨服务复用）

`application.yml`、拦截器配置**零改动**（新路径天然落在 `/api/v1/**` JWT 保护范围）。

---

## 2. 前端：frontend/

### 2.1 技术选型

Vite 6 + React 19 + TypeScript(strict)，唯一功能依赖 `react-markdown`（安全渲染，
不走 dangerouslySetInnerHTML）。无路由库——三个页签用状态切换足够；
无 UI 组件库——手写 CSS（`styles.css`，约 400 行）。

### 2.2 结构

```
frontend/
├── vite.config.ts     # /api 代理到后端（默认 8080，API_PROXY 环境变量可覆盖）
├── index.html
└── src/
    ├── main.tsx       # 入口
    ├── App.tsx        # 认证门 + 顶栏 + 页签切换 + 当前仓库状态
    ├── api.ts         # fetch 封装：自动带 Bearer；401 先 refresh（一次性轮换）再重试；
    │                  # 刷新失败清登录态并广播 ds-logout
    ├── types.ts       # 与后端 DTO 对齐
    ├── Login.tsx      # 登录表单
    ├── Vaults.tsx     # 仓库列表/新建/选中（选中项存 localStorage）
    ├── Tokens.tsx     # 令牌列表/签发/撤销；签发弹窗展示明文一次（复制按钮 + 警示）
    ├── Records.tsx    # 月历（周一开头 42 格）→ 点有徽标的日期 → 记录 chips → 内容渲染
    └── styles.css
```

### 2.3 关键设计

- **令牌自动续期**：accessToken 默认 120 分钟；api.ts 收到 401（非 auth 接口）先用
  refreshToken 调 `/auth/refresh` 换新（后端轮换语义：旧 refresh 立即作废），
  成功则重放原请求，失败才登出。用户正常使用期间无感。
- **登录态**：`localStorage["daily-sync-auth"]`（accessToken/refreshToken/username）；
  `ds-logout` 事件让任意请求路径的失败都能把 UI 打回登录页。
- **当前仓库**：`localStorage["daily-sync-vault-id"]`，顶栏 chip 常显，三个页签共享。
- **日历打点**：按可视月历的 42 格范围请求 `records/dates`（前后补位周也覆盖），
  `count>0` 的日期高亮 + 右上角徽标；今日描边；无记录日期禁用。
- **明文令牌仅一次**：签发响应的 `token` 只进弹窗（readonly textarea + 复制按钮 +
  “关闭后无法再次查看”警示），不落任何存储。

### 2.4 运行

```bash
cd frontend
npm install
npm run dev            # http://localhost:5173，/api 代理到 localhost:8080
API_PROXY=http://localhost:8081 npm run dev   # 代理到其他端口（如测试实例）
npm run build          # tsc -noEmit + vite build → dist/（M6 再接 Spring Boot static）
```

---

## 3. 验证记录（2026-09-10，全部通过）

### 3.1 后端（curl 对 8081 测试实例）

- `records/dates`：vault 8（obsidian-test）返回 `09-05 → 3`、`09-06 → 2`，与上传文件一致
- `records?date=2026-09-05`：3 条含全文，路径升序
- 不存在的仓库 → 404

### 3.2 前端（浏览器实操，vite dev × 8081 × 真实数据）

1. 登录 shuiyi → 进入主界面，默认选中 main，日历显示 9/9（1 篇）
2. 切到 obsidian-test → 日历 9/5 徽标 3、9/6 徽标 2；今日（9/10）描边
3. 点 9/5 → 3 个文件 chips（你好/天气/项目周报）→ 切换查看，Markdown 渲染正常
4. 令牌页：5 枚令牌状态正确（含"我的电脑"最近使用时间——插件仍在后台同步）；
   签发"前端测试"→ 弹窗展示明文 + 复制；撤销（confirm 弹窗）→ 服务端 status=0
5. 退出登录 → 回登录页
6. 截图目检：布局整洁无错乱

测试痕迹：vault 8 留有已撤销的 verify / 前端测试 / server-check / hash-check 令牌
（status=0，无害）；"我的电脑"为唯一活跃令牌。

---

## 4. 常见问题（FAQ）

| 现象 | 原因与处理 |
| --- | --- |
| 前端页签空白/接口 404 | 后端还没重启到含查询接口的版本——IDEA 里重启后端即可（新接口是 M4 加的） |
| 登录后一瞬间被踢回登录页 | refreshToken 也过期了（30 天未使用），正常重新登录 |
| 日历某天没徽标但本地有日记 | 该文件名首段不是 `YYYY-MM-DD`（如周记 `2026-W37`、`测试代码.md`）——record_date 为 null，设计如此 |
| 日历数据不新鲜 | 插件约 5 分钟自动同步一次；等下一轮或点 Obsidian 状态栏立即同步后刷新页面 |
| 令牌弹窗关了还想再看 | 看不了——明文只在签发时返回一次（库里只有哈希）。撤销重签一枚即可 |
| 想在手机上用 | 同一局域网访问 `http://电脑IP:5173`（vite 默认只听 localhost，需 `--host`）；生产化等 M6 |

---

## 5. 验收清单与下一步

M4 完成标准：

- [x] 后端两个查询接口实现 + curl 验证（含 404）
- [x] `mvn compile` 通过
- [x] 前端 `npm run build`（tsc strict）通过
- [x] 浏览器全流程实操：登录/仓库切换/日历打点/日记阅读/令牌签发/撤销/退出
- [x] 截图目检布局
- [x] 提交：`M4: Web 前端（登录/仓库/令牌/日记浏览）+ 按日查询接口`

**注意**：您 IDEA 里运行的后端要**重启一次**才会带上新的查询接口
（`GET /api/v1/vaults/{id}/records*`），然后 `cd frontend && npm run dev` 即可使用。

**M5/M6 预告**：M5 收尾加固（附件同步评估、审计日志、限流）；M6 部署
（docker-compose 上云 120.26.58.22，前端 dist 塞 Spring Boot static，`.env` 接管配置）。
