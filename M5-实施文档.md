# Daily Sync M5 实施文档 —— 收尾加固（限流 / 审计日志 / 附件同步评估）

> 目标：给裸奔的接口补上两道护栏——**限流**（防密码爆破、防同步接口滥用）与
> **审计日志**（谁在什么时候从哪个 IP 登录/签发/撤销了什么，安全事件有据可查）；
> 同时把搁置已久的**附件同步**做完技术评估（结论：M6 部署后再决定，推荐方案已定型）。
>
> 本期后端新增 8 个文件、改 8 个；前端新增 1 页、改 3 个文件；建 1 张表。

---

## 0. 本期范围

**做什么：**

- 限流（进程内固定窗口，无第三方依赖）：
  - `/api/v1/auth/**`（登录/注册）：按客户端 IP，默认 10 次/分钟——防密码暴力破解
  - `/api/v1/sync`：按同步令牌哈希（无令牌按 IP），默认 120 次/分钟——防令牌爆破/滥用
  - 超限返回 429 + `Retry-After` 头；被拒请求不消耗数据库（限流拦截器排在鉴权之前）
- 审计日志（audit_logs 表，只增不改）：
  - 事件：注册 / 登录成功 / 登录失败（含尝试的用户名）/ 创建仓库 /
    签发令牌 / 撤销令牌 / 同步鉴权失败（已撤销令牌被复用等，能归属到用户）
  - 查询接口 `GET /api/v1/audit-logs`（JWT，只能看自己的，新→旧 id 游标翻页）
  - 前端新增「日志」页签：事件标签（失败类红色）+ 详情 + 来源 IP + 加载更多
- 客户端 IP 解析：直连取 remoteAddr；`behind-proxy` 开关打开后取 X-Forwarded-For
  首段（M6 反代后开）。默认关闭——该头可伪造，直连公网时盲信会被绕过限流

**不做什么（M6）：**

- ❌ 附件同步实现（本期只做评估，见第 6 节）
- ❌ 生产部署（docker-compose 上云、`frontend/dist` 塞 Spring Boot static、`.env` 接管配置）
- ❌ 分布式限流（Redis 等）——单实例部署，进程内计数够用；重启清零可接受

---

## 1. 建表（云服务器 MySQL，幂等）

```bash
mysql -h120.26.58.22 -u daily_sync -p daily_sync
```

```sql
-- 审计日志：只增不改；user_id 可空（无法确认身份的事件）
CREATE TABLE IF NOT EXISTS audit_logs (
  id         BIGINT       PRIMARY KEY AUTO_INCREMENT,
  user_id    BIGINT       NULL,                 -- 操作者；登录失败但用户存在时可回填
  vault_id   BIGINT       NULL,                 -- 与仓库相关的事件才有
  action     VARCHAR(32)  NOT NULL,             -- REGISTER / LOGIN_SUCCESS / ...
  detail     VARCHAR(255) NOT NULL DEFAULT '',  -- 人读摘要，不含敏感值
  ip         VARCHAR(45)  NOT NULL DEFAULT '',  -- IPv6 最长 45
  created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP, -- 应用侧显式写入（见 3.3 时区坑）
  KEY idx_user_id (user_id, id),                -- 按用户 + id 游标翻页
  KEY idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

> 不存令牌/密码的任何明文或哈希；detail 里只有备注名、令牌 id、尝试的用户名。

---

## 2. 限流

### 2.1 组件

| 文件 | 职责 |
| --- | --- |
| `common/RateLimiter.java` | 固定窗口计数器（ConcurrentHashMap + compute 原子换窗）；`@Scheduled` 每 5 分钟清过期窗口防 key 无限增长 |
| `common/RateLimitInterceptor.java` | 挂 `/api/v1/auth/**` 与 `/api/v1/sync`；auth 按 IP、sync 按 `sha256(X-Sync-Token)`（现算不查库）；超限 429 + Retry-After |
| `common/ClientIp.java` | 客户端 IP 解析，`behind-proxy` 开关控制是否信任 X-Forwarded-For |

### 2.2 配置（application.yml）

```yaml
daily-sync:
  rate-limit:
    enabled: ${RATE_LIMIT_ENABLED:true}   # 本机联调可关
    auth-per-minute: 10                   # 登录/注册：每 IP
    sync-per-minute: 120                  # 同步：每令牌；插件 5 分钟一轮 push+pull 绰绰有余
  behind-proxy: ${BEHIND_PROXY:false}     # M6 反代后改 true
```

### 2.3 设计取舍

- **固定窗口**而非滑动窗口：边界处理论突发 2 倍（59s 和 61s 各打满），对防爆破足够，
  实现只有 30 行。`AppConfig` 加了 `@EnableScheduling`（清理任务需要）。
- **sync 维度选令牌哈希而非 IP**：一台设备换网络（IP 变）不影响；多设备同一 NAT 出口
  也不会互相挤占配额。哈希直接对请求头现算，乱猜令牌的洪水连数据库都不碰。
- **注册顺序**：RateLimitInterceptor 注册在最前（`WebMvcConfig`），被拒请求零 DB 查询。

---

## 3. 审计日志

### 3.1 组件

| 文件 | 职责 |
| --- | --- |
| `entity/AuditLog.java` / `mapper/AuditLogMapper.java` | 表映射 |
| `service/AuditService.java` | `record()` 尽力而为写入（失败仅告警不打断主流程）+ `Action` 枚举 + `list()` 按 user_id 的 id 游标翻页 |
| `controller/AuditLogController.java` | `GET /api/v1/audit-logs?before=&limit=`（limit 收敛 1~100，默认 50；返回条数 < limit 即末页） |
| `dto/AuditLogResponse.java` | id / userId / vaultId / action / detail / ip / createdAt |

### 3.2 事件与埋点位置

| 事件 | 埋点 | detail |
| --- | --- | --- |
| REGISTER | AuthService.register | 用户名: xxx |
| LOGIN_SUCCESS / LOGIN_FAIL | AuthService.login | 失败记「尝试用户名: xxx」（用户存在时能回填 user_id） |
| VAULT_CREATE | VaultService.create | 仓库名: xxx |
| TOKEN_ISSUE / TOKEN_REVOKE | VaultService | 令牌 #id（备注名）——永不记明文/哈希 |
| SYNC_AUTH_FAIL | SyncTokenInterceptor | 已撤销令牌被复用 / 仓库不存在 / 账号被禁用 |

IP 由 Controller/拦截器用 `ClientIp` 解析后显式传给 Service（服务层不碰 Servlet API）。

**两个刻意的设计**：

- 乱猜的随机令牌（查表即空）**不写审计**——纯噪音且可刷库，限流器已挡频率；
  只有「查得到这枚令牌」的失败才记，且尽量归属到仓库主人（审计页才看得见）。
- 无法归属用户的行（user_id NULL，如账号已删的边缘情况）不出现在任何人的列表里。

### 3.3 时区坑（本期发现并规避）

云上 MySQL 容器时区是 **UTC**（`SELECT @@system_time_zone` = UTC），DATETIME 的
`DEFAULT CURRENT_TIMESTAMP` 会存成 UTC，读回来比本地差 8 小时。vaults/daily_records
一直是应用侧 `LocalDateTime.now()` 显式写入所以无恙。**audit_logs 同样显式写入**
（`AuditService.record()` 里 set createdAt）；DDL 里的默认值只作兜底。
M6 部署时可顺手把 MySQL 容器时区固定为 Asia/Shanghai。

---

## 4. 前端

- `types.ts`：`AuditLog` 接口
- `AuditLogs.tsx`：「日志」页签——事件中文标签（LOGIN_FAIL/SYNC_AUTH_FAIL 红色
  `tag-danger`，未知类型兜底显示原字符串）、详情、来源 IP、`before=<最后一条 id>`
  游标「加载更多」、刷新按钮、末页提示
- `App.tsx`：TABS 加 `{ key: "audit", label: "日志" }`（该页与仓库选择无关，无需 vaultId）
- `styles.css`：`.tag-danger` / `.audit-time` / `.audit-detail` / `.audit-ip` / `.audit-actions`

---

## 5. 验证记录（2026-09-10，全部通过；8082 测试实例，测试数据已清理）

### 5.1 限流

- 登录限流：同 IP 第 1~10 次放行，第 11 次起 429 + `Retry-After: 27`，body
  `{"code":429,"message":"请求过于频繁，请稍后再试"}`
- 同步限流（临时把 sync-per-minute 调成 5 复验）：同令牌 5×401（鉴权拒绝）后 429；
  换一枚令牌立即恢复——**按令牌隔离**验证通过
- 踩坑记录：用默认 120/分钟压测 125 次全放行——单次请求 ~0.17s，整轮 ~21s 恰好跨过
  分钟边界，计数被拆进两个窗口（固定窗口的已知特性，非 bug）；验证限流行为要用小阈值

### 5.2 审计日志（curl）

- 七类事件全量落库，新→旧返回正确；`before=10` 游标翻页返回空（此前是 NULL-user 行，
  按设计不可见）；无 token 访问 401
- 已撤销令牌复用 → SYNC_AUTH_FAIL 且 user_id 正确归属到仓库主人（撤销后多查一次
  vaults 就是为了这个归属）
- LOGIN_FAIL 的 detail 带尝试用户名、user_id 在用户存在时回填
- 时间戳：修复后新事件为本地时间（12:47:37），修复前的行是 UTC（04:44）——对比可证

### 5.3 前端（浏览器实操）

- 注册 m5_demo → 登录 → 「日志」页签：3 行（登录成功 / 登录失败 / 注册），
  失败行红色标签，时间为本地时间，「已到最早记录」提示正常
- 截图目检：表格对齐、标签配色、无截断错乱
- 提醒：本机 5173 是旧会话的 vite（代理到 8080 旧后端），验证时新起了 5175（API_PROXY=8082）——
  **IDEA 里的后端要重启到 M5 版本，旧后端没有 /api/v1/audit-logs，页面会报 500**

### 5.4 回归

- 有效令牌 push（updated）/ 重复 push（unchanged）/ pull 全部正常——限流拦截器不影响同步语义
- JWT 接口（vaults/audit-logs）不受限流影响（只挂 auth/** 与 sync）

---

## 6. 附件同步评估（本期只评估不实现）

**现状**：只同步 `.md`（M2 设计如此），日记里的 `![[截图.png]]` 引用原样保留在正文，
图片文件本身不上云——换设备打开日记图片裂开。

**方案对比**：

| 方案 | 做法 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- | --- |
| A. Base64 内嵌 | 附件塞进现有 JSON push | 零新接口 | 体积 +33%，1MB 限制下只够小图；大文件撑爆内存与 MEDIUMTEXT | ❌ 只适合应急 |
| B. 内容寻址 + 本地磁盘 | 独立 multipart 上传接口，`sha256` 作文件名存 `data/attachments/`；`attachments` 表记 (vault_id, path, sha256, size, deleted)；md 引用不动 | 天然去重（同哈希同 blob）；文件名即哈希无路径穿越；单机 TB 级容量 | 需要清理策略（墓碑+引用计数 GC）；多实例后要共享盘 | ✅ **推荐** |
| C. 对象存储 OSS/S3 | B 的存储层换成云对象存储 | 弹性、免运维磁盘 | 引入依赖与费用；个人规模用不上 | 暂不需要 |

**推荐方案 B 的协议草案**（实现约一天，放 M7 或 M6 后按需启动）：

- 上传：`POST /api/v1/sync/attachments`（multipart，X-Sync-Token 鉴权），
  服务端算 sha256 落盘 + upsert 表行；同哈希重复上传秒回（幂等）
- 下载：`GET /api/v1/vaults/{id}/attachments/{path}`（JWT，前端日记页渲染图片用）
- 删除：沿用 push 协议墓碑语义，`deleted=1` 后台任务定期做引用计数 GC
- 安全上限：单文件 10MB（png/jpg/gif/webp/pdf 白名单）、每仓库配额 1GB
- 插件端：同步时扫描 md 引用的本地附件，缺失（按 sha256 比对）即上传；拉取时反向补齐

---

## 7. 常见问题（FAQ）

| 现象 | 原因与处理 |
| --- | --- |
| 日志页报「服务器内部错误」 | IDEA 里的后端还是 M4 版本（没有该接口），**重启后端**即可 |
| 登录时提示请求过于频繁 | 同一 IP 一分钟内登录/注册超过 10 次（含失败）；等 `Retry-After` 秒数或 `RATE_LIMIT_ENABLED=false` 重启 |
| 审计里看不到乱猜令牌的探测 | 设计如此：无法归属用户的失败不写审计（防刷库），被限流器挡着；确需排查直接查表 `SELECT * FROM audit_logs WHERE user_id IS NULL` |
| 审计时间是 UTC 差 8 小时 | 旧数据（M5 之前依赖 DB 默认值的表）如此；M5 起审计行由应用侧写本地时间；M6 顺手把 MySQL 容器时区改为 Asia/Shanghai |
| 想临时关闭限流 | `RATE_LIMIT_ENABLED=false` 环境变量重启后端 |
| 插件会触发限流吗 | 不会：5 分钟一轮 push+pull 远低于 120/分钟；多设备也各按各的令牌计数 |

---

## 8. 验收清单与下一步

M5 完成标准：

- [x] audit_logs 建表（云 MySQL）
- [x] 限流：登录 10/min/IP、同步 120/min/令牌，429 + Retry-After，curl 实测触发与隔离
- [x] 审计：七类事件 curl 验证；查询接口 + 游标翻页 + 401
- [x] 时区坑发现与修复（应用侧显式写 createdAt）
- [x] 前端「日志」页 + `npm run build`（tsc strict）+ 浏览器实操 + 截图目检
- [x] `mvn compile` 通过；测试数据（m5_test / m5_demo）已从库中清理
- [x] 附件同步评估成文（第 6 节，推荐方案 B）
- [ ] 提交：`M5: 收尾加固（限流/审计日志）+ 附件同步评估`

**M6 预告**：docker-compose 上云 120.26.58.22（后端 + 已有 MySQL），`frontend/dist`
塞 Spring Boot static，`.env` 接管配置注入（DB_PASSWORD/JWT_SECRET/INVITE_CODE/
BEHIND_PROXY=true），Jenkinsfile 落地。

**重启提醒**：IDEA 里的后端需重启一次才有限流与审计接口；重启后旧插件无需任何改动。
