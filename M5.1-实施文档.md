# Daily Sync M5.1 实施文档 —— 同步凭证改造（账号密码 + 仓库自动创建）

> 动机：手动建仓库 + 手动签发令牌的流程太绕——Web 建仓、复制 dst_ 令牌、粘进插件，
> 仓库名对不上、令牌撤销了设备全掉线。本期把这条路整个拆掉：
> **插件只填账号密码，仓库按 Obsidian 库名自动创建**；Web 端顶栏加仓库下拉框随时切换。
>
> 后端删 8 个文件、改 8 个；前端删 1 页、改 4 个；插件改 2 个 + 测试脚本。

---

## 0. 变更总览

| 项 | 之前（M2~M5） | 现在（M5.1） |
| --- | --- | --- |
| 同步鉴权 | `X-Sync-Token: dst_…`（独立令牌体系） | 账号密码登录换 JWT（`Authorization: Bearer`），refreshToken 30 天轮换续期 |
| 仓库创建 | Web 端手动新建 + 令牌绑定 | 插件按 Obsidian 库名 `?vault=` 自动创建（find-or-create） |
| Web 仓库页 | 列表 + 新建表单 | 只读列表 |
| Web 顶栏 | 「仓库：xxx」静态徽标 | 下拉框随时切换当前仓库 |
| 同步令牌页 | 签发/列表/撤销 | **整页删除**（接口同步下线） |

**为什么是登录换 JWT 而不是每个请求带密码**：密码无字符集限制（可中文），
HTTP 头只支持 ISO-8859-1，放头必乱码；JSON 请求体天然 UTF-8 安全。
且密码只在登录瞬间上网络（120 分钟才需要续一次，续期走 refresh 不传密码），
登录接口自带 10 次/分钟/IP 限流 + LOGIN_FAIL 审计，防爆破现成。

---

## 1. 同步协议变化（后端）

```
POST /api/v1/auth/login            {"username","password"} → accessToken + refreshToken
POST /api/v1/auth/refresh          {"refreshToken"}        → 新令牌对（旧的立即作废）
GET  /api/v1/sync?vault=&since=&limit=    Authorization: Bearer <accessToken>
POST /api/v1/sync?vault=                   Authorization: Bearer <accessToken>
```

- `/api/v1/sync` 收归 **AuthInterceptor** JWT 保护（WebMvcConfig 去掉 exclude），
  `SyncTokenInterceptor`/`SyncContext`/`SyncToken` 实体/Mapper/三个令牌 DTO 全部删除
- **vault 参数**：目标仓库名（= Obsidian 库名）。`VaultService.findOrCreateVault(userId, name)`：
  查 `(user_id, name)` → miss 则插入（新仓库 version=0，记审计 `VAULT_CREATE`，
  detail 注明「插件同步自动创建」）→ 并发首同步撞 `uk_user_name` 唯一键时重查拿现成的
- 名称规则：trim 后 1-64 字符，否则 400；缺 `vault` 参数 400
- 删除接口：`POST /api/v1/vaults`（手动建仓）、`POST/GET /vaults/{id}/tokens`、
  `DELETE /vaults/{id}/tokens/{tokenId}`——405/404（见 §3 异常处理补充）
- 限流：sync 桶的 key 从令牌哈希改为客户端 IP（JWT 校验是纯 HMAC 无 DB 开销）
- 审计枚举退役：`TOKEN_ISSUE` / `TOKEN_REVOKE` / `SYNC_AUTH_FAIL`（audit 表当时 0 行，无历史包袱）

**数据库**：`sync_tokens` 表保留不动（已废弃，无代码引用；确认稳定后可手动 DROP）。

## 2. 插件端（quick-daily-note）

- 设置：删除「同步令牌」字段（连同 dst_ 前缀告警），新增「账号」「密码」两栏
  （存本机 data.json，不写入库内文件）
- `sync.ts` 鉴权链（全部在 syncing 锁内串行，无并发抢号）：
  1. 内存有 accessToken 直接用
  2. 401 / 无缓存 → 手头 refreshToken 调 `/auth/refresh`（一次性轮换，新对即刻持久化）
  3. refresh 失效（过期/被轮换作废）→ 账号密码 `/auth/login`；登录 401 才报
     「账号或密码错误，请在插件设置中更新」（Notice 去重，不轰炸）
- 请求 URL 附 `vault=<encodeURIComponent(库名)>`（`app.vault.getName()` 经 host 注入）
- headless 集成测试（`scripts/run-sync-test.mjs`）改为**自注册测试账号**（不依赖真实用户），
  8 场景 23 断言全过：自动建仓/范围过滤/防抖推送/双设备/冲突本地胜/墓碑/离线删除/分页 521 条/错密码

## 3. 全局异常处理补充（顺手修的缺口）

验证时发现三个未兜底的异常都会 500，补了 handler：

| 异常 | 场景 | 现在返回 |
| --- | --- | --- |
| `MissingServletRequestParameterException` | sync 缺 `vault` 参数 | 400「缺少必填参数: vault」 |
| `NoResourceFoundException` | 访问已下线的旧接口 | 404「接口不存在」 |
| `HttpRequestMethodNotSupportedException` | 对只读接口 POST（如建仓旧入口） | 405「请求方法不支持」 |

## 4. 前端

- 顶栏 `vault-chip` → `<select class="vault-select">`（列出全部仓库、切换即存 localStorage）
- 仓库页：删除新建表单，只读列表 + 提示「仓库由插件首次同步时按库名自动创建」
- localStorage 恢复选中仓库的逻辑从 Vaults.tsx 移入 App.tsx 的 reloadVaults
- 删除 Tokens.tsx 与页签；types.ts 清理；AuditLogs 标签映射同步精简

## 5. 验证记录（2026-09-10，全部通过）

- 后端 curl（8082 实例）：无 JWT push 401；中文仓库名（URL 编码）push 自动建仓 + 审计行；
  幂等重推 unchanged；pull 正常；缺 vault/超 64 字符/纯空格 → 400；旧接口 → 404/405
- 插件 headless：23/23 通过（见 §2）
- 前端：tsc strict build 通过；浏览器实操（8082 × vite）：登录 → 顶栏下拉框显示并选中
  「我的日记库」→ 仓库页只读无表单 → 令牌页已消失 → 日历打点正常；截图目检布局协调
- 测试数据（m51_test / plugtest_* 用户、仓库、审计行）已全部清理

## 6. 常见问题（FAQ）

| 现象 | 原因与处理 |
| --- | --- |
| 旧版插件同步报 401 | 旧版还在用 X-Sync-Token——**更新插件**（M5.1 起 Bearer 鉴权），设置里改填账号密码 |
| Obsidian 里改了库名 | 下次同步会按新名字**新建一个云端仓库**（旧仓库原样保留）；想续用旧数据把库名改回去 |
| 想删掉废弃的 sync_tokens 表 | 确认所有设备都用新版插件后：`DROP TABLE sync_tokens;`（代码已无引用） |
| 日志页没有「签发/撤销令牌」了 | 令牌体系已退役；现在只有 注册/登录成败/仓库自动创建 四类事件 |
| 插件多久要重新输密码 | 正常永不——refreshToken 30 天滚动轮换；只有连续 30 天不用、或服务端改了密码才会要求重输 |

## 7. 验收清单

- [x] 后端：令牌体系删除、sync 接 JWT、vault find-or-create、异常处理 400/404/405
- [x] `mvn compile` + curl 全项验证
- [x] 插件：设置改造 + 鉴权链 + vault 参数；`npm run build` + headless 23/23
- [x] 前端：下拉框/只读仓库页/删令牌页；build + 浏览器实操 + 截图
- [x] 测试数据清理；提交（daily-sync + quick-daily-note 两仓库）

**下一步（M6，提前启动）**：Docker 化 + GitHub Actions 自动构建部署到 120.26.58.22
（前端 dist 塞 Spring Boot static、`.env` 接管配置、BEHIND_PROXY=true）。
