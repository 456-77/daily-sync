# Daily Sync M6 实施文档 —— GitHub Actions 自动构建部署上云

> 目标：push 到 master 即自动完成「构建镜像 → 上传服务器 → 重启服务」全流程，
> 后端 + 前端（同源塞进 Spring Boot static）一个容器跑在 120.26.58.22 上。
> Jenkins 暂缓——个人规模一条 Actions 流水线足够，零运维成本。

---

## 0. 部署形态

```
GitHub push ──► Actions（ubuntu runner）
                ├─ docker build（三阶段：node 前端 → maven 后端 → JRE 运行时）
                ├─ docker save | gzip → scp 到服务器 /opt/daily-sync/
                └─ ssh：docker load && docker compose up -d && 冒烟 healthz
服务器（CentOS 7, 1.7G 内存）
  ├─ daily-sync 容器（:8080，限内存 480m，连宿主 MySQL）
  ├─ mysql 容器（已有，:3306）
  └─ redis 容器（已有，:6379）
```

访问入口：`http://120.26.58.22:8080`（阿里云安全组已放行 TCP 8080）。
Obsidian 插件的服务端地址填这个。

## 1. 镜像（Dockerfile，三阶段）

| 阶段 | 基础镜像 | 产物 |
| --- | --- | --- |
| frontend-build | node:20-alpine | `frontend/dist`（先 `npm ci` 装依赖吃层缓存） |
| backend-build | maven:3.9-eclipse-temurin-17 | dist 塞进 `src/main/resources/static` 后 `mvn package`（阿里云镜像源见 `docker/settings.xml`） |
| runtime | eclipse-temurin:17-jre | 仅 jar；`TZ=Asia/Shanghai`（审计时间戳）；`-XX:MaxRAMPercentage=60` |

前端与 API 同源 → 无 CORS、无 vite 代理，浏览器直接开。静态资源不经 JWT
拦截器（AuthInterceptor 对非 HandlerMethod 直接放行），`/api/v1/**` 照旧鉴权。

## 2. 服务器编排（docker-compose.yml）

- `DB_HOST=host.docker.internal`（`extra_hosts: host-gateway` 映射宿主机，Linux 专用），
  连宿主上已有的 MySQL 容器的 3306；`application.yml` 的 DB_HOST/DB_PORT 已环境变量化
- 敏感配置在 `/opt/daily-sync/.env`（chmod 600，不入库）：DB_PASSWORD / JWT_SECRET /
  INVITE_CODE / BEHIND_PROXY
- `mem_limit: 480m`：宿主机仅 1.7G（MySQL/Redis 占了一半），限死容器防 OOM；
  另加了 2G swapfile 兜底（`/swapfile`，已写 fstab）
- healthcheck：镜像里没 curl/wget，用 bash `/dev/tcp` 发裸 HTTP 探 `/healthz`

## 3. Actions 流水线（.github/workflows/deploy.yml）

- 触发：push 到 master / 手动 workflow_dispatch；`concurrency: deploy` 串行防撞
- Secrets（Settings → Secrets and variables → Actions）：`SSH_HOST` / `SSH_USER` /
  `SSH_KEY`（专用部署密钥 `~/.ssh/daily_sync_deploy`，ed25519；公钥在服务器
  /root/.ssh/authorized_keys，标注 daily-sync-deploy）
- 步骤：build → save tar.gz → scp → ssh（load + `compose up -d` + prune）→ 冒烟
  `curl /healthz`
- 镜像走 tar 传输，**不依赖镜像仓库**（ACR 免配置；镜像 ~350MB，gz 后 ~120MB，
  单次部署上传约 1~2 分钟）
- 踩坑记录：runner 上 `~/.ssh` 目录不存在，`install` 建不了密钥文件——先
  `mkdir -p ~/.ssh`（首版在此挂过一次）

## 4. 变更配套（本期顺手做）

- `application.yml`：datasource 地址改为 `${DB_HOST:120.26.58.22}:${DB_PORT:3306}`
- `.env.example`：补 BEHIND_PROXY 说明（直连公网保持 false；将来套 nginx 反代后改 true，
  届时客户端 IP 取 X-Forwarded-For，限流/审计才准确）

## 5. 验证记录（2026-09-10，全部通过）

1. Actions run #2 全绿：build 2.5 分钟 → scp → 服务器 load + up → 冒烟 OK
2. 服务器本地：容器 Up（health: starting → healthy）、`/healthz` ok、内存 1.3G/1.7G（正常）
3. 公网 E2E（安全组放行 8080 后）：前端 index.html ✓ / 注册 ✓ / 登录换 JWT ✓ /
   中文仓库名同步推送自动建仓 ✓（vault 14「生产验收库」）/ 拉取 ✓ /
   审计 REGISTER + VAULT_CREATE 落库 ✓ / 坏 token 401 ✓
4. 验收数据已清理（m6_verify 用户、生产验收库及全部关联行）
5. 再次 push 触发的重部署幂等（同 tag `daily-sync:latest` 覆盖 load，服务无状态无感）

## 6. 日常操作

| 场景 | 操作 |
| --- | --- |
| 发布新版本 | `git push`（master）→ 自动全流程；Actions 页看进度 |
| 回滚 | 服务器 `docker tag` 旧镜像/重新 load 旧 tar 后 `docker compose up -d` |
| 看日志 | 服务器 `docker logs -f daily-sync`（或 `--tail 200`） |
| 改配置 | 编辑 `/opt/daily-sync/.env` → `docker compose up -d` 重启生效 |
| 手动触发 | Actions → Build & Deploy → Run workflow |
| 临时下线 | 服务器 `docker compose down`（数据都在 MySQL，容器无状态） |

## 7. 安全清单（建议尽快处理）

- [ ] **root 密码**：会话中出现过，建议 `passwd` 改掉；部署已改走专用密钥，不依赖密码
- [ ] **3306 对公网开放**：MySQL 端口全网可达风险较高，建议安全组收窄为常用 IP，
      或给 daily_sync 用户加 IP 白名单（`user@'%'` → `user@'1.2.3.4'`）
- [ ] **JWT_SECRET / DB_PASSWORD**：真实值只在服务器 .env（600）与本地 .env，
      仓库里是默认占位符；但**历史提交里含早期 DB 密码默认值**，若仓库转公开需先改密
- [ ] **注册开关**：公网可达后建议在 .env 配 INVITE_CODE 关闭开放注册
- [ ] HTTPS：目前明文 HTTP，插件密码登录走公网有暴露风险；后续套 nginx + 证书
      （Let's Encrypt）+ BEHIND_PROXY=true

## 8. 下一步候选

- 附件同步（M5 评估的方案 B：sha256 内容寻址 + multipart 上传）
- nginx + 域名 + HTTPS + BEHIND_PROXY
- 前端「同步设置引导页」：展示服务端地址 + 一键复制，方便新设备配置插件
