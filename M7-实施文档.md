# Daily Sync M7 实施文档 —— 附件同步（粘贴图片 / PDF）

> 目标：把日记里 `![[截图.png]]` 引用的图片真正同步上云，换设备打开日记不再图裂。
> M5 第 6 节做过技术评估并定型方案 B（sha256 内容寻址 + 独立表 + 磁盘文件），本期按它落地。
>
> 三个已确认的产品口径：
> 1. **只同步被日记引用到的附件**（不镜像库里所有二进制）；
> 2. **图片 + pdf，单文件 10MB**（不含 svg，见第 8 节）；
> 3. **网页端只读展示与下载**（不能上传/删除，正文仍只读）。

---

## 0. 数据形态：字节在磁盘，元数据在库

| 存什么 | 存在哪 |
| --- | --- |
| 附件字节 | 磁盘，按内容寻址：`<storage.dir>/<vaultId>/<sha256 前两位>/<sha256>` |
| 附件元数据 | MySQL `attachments` 表（路径、文件名、sha256、字节数、墓碑、版本号） |

两边靠 `sha256` 关联。**不把字节塞进数据库**（`LONGBLOB` 方案）：宿主机只有 1.7G 内存且已跑
MySQL/Redis，几百 MB 图片会挤占 buffer pool 把正文查询拖慢，`mysqldump` 备份体积也会从 KB 涨到 GB。
代价是需要挂一个宿主机目录（见第 6 节）。

路径落点分三种场景，都是同一个相对配置 `daily-sync.storage.dir` 算出来的：

| 场景 | 实际路径 |
| --- | --- |
| 容器内 | `/app/data/attachments/<vaultId>/...` |
| 服务器宿主机 | `/opt/daily-sync/data/attachments/<vaultId>/...` |
| 本地 IDEA 跑后端 | `backend/data/attachments/<vaultId>/...` |

> **为什么按仓库分目录**：仓库边界落在目录上之后，「删用户/删仓库」变成自包含操作
> （删完行 `rm -rf <dir>/<vaultId>` 即可），不需要任何引用计数，也不可能误删别人的数据；
> `du -sh` 也能直接看出某个仓库占了多少。代价是**放弃跨仓库去重**——同一张图传到两个仓库
> 会各存一份。个人日记场景这种情况很少，值得。（仓库内去重仍然生效：同一张图被多篇日记
> 引用只存一份。）

---

## 1. 协议

附件与正文**共用同一个 `vaults.version` 计数器**，所以客户端只有一条拉取游标：

| 用途 | 接口 | 寻址 |
| --- | --- | --- |
| 上传/覆盖（插件） | `POST /api/v1/sync/attachments?vault=<库名>&path=<路径>`，body 为**原始字节** | 库名 |
| 墓碑删除（插件） | `DELETE /api/v1/sync/attachments?vault=<库名>&path=<路径>` | 库名 |
| 下载（插件） | `GET /api/v1/sync/attachments?vault=<库名>&path=<路径>` | 库名 |
| 下载（网页端） | `GET /api/v1/vaults/{id}/attachments?path=<路径>` 或 `?name=<文件名>` | 仓库 id |

- 插件只知道库名（与 `/api/v1/sync` 一致，上传时按名 find-or-create 仓库）；
  网页端只有仓库 id（与 `/api/v1/vaults/{id}/records` 一致，归属校验走 `VaultService.ownedVault`）。
  两条下载入口刻意写在同一个 Controller 里，安全响应头只写一遍，避免两处漂移。
- **上传用原始字节流而不是 multipart**：Obsidian 的 `requestUrl` 直接吃 ArrayBuffer，
  multipart 边界得在插件里手搓；原始流还能边读边哈希边计数，天然流式，也省掉
  `spring.servlet.multipart` 的配置。后端从 `HttpServletRequest.getInputStream()` 读，
  不做 `byte[]` 全量缓冲（容器堆只有约 290M）。
- `?name=` 是给网页端的 `![[截图.png]]` 用的（这种写法不带目录，Obsidian 按文件名在全库找）。
  解析规则放在服务端一处：**同目录优先 → 路径最短 → 字典序**，插件与网页端共享，
  网页端因此不用维护「文件名 → 路径」映射。精确路径没命中时也会退化成按文件名找一次——
  库里同一张图可能被写成「相对笔记」或「相对库根」，两种约定都兜住。

### 1.1 为什么两表归并不会歧义

`push` 是「`version = version + 1` 后回读」，InnoDB 行锁保证并发自增各不相同；正文一批占一个
版本号，附件一次上传（一条一个请求）占一个版本号。因此**同一个版本号下只会出现同一类行**，
`pull` 按 version 归并两张表即可，不会跨表撞车。

`pull` 的实现：两张表各取 `limit + 1` 条，归并按 version 升序截断到 `limit`，
`hasMore = 归并后总数 > limit`。页尾必然是本页最大版本号，客户端沿用既有的
`since = 页尾 version - 1` 翻页规则（同版本组会被整组重取，按 path 去重，与 M2 一致）。

响应新增字段（只加不改，老客户端忽略即可）：

```
SyncPullResponse { vaultVersion, hasMore, records: [...], attachments: [
  { path, name, sha256, size, deleted, version }   // 不含字节，要字节另去下载接口取
]}
```

---

## 2. 数据库

无 migration 工具，DDL 手工执行（与其他表一致）。本次已在云端 MySQL 执行：

```sql
CREATE TABLE IF NOT EXISTS attachments (
  id         BIGINT       PRIMARY KEY AUTO_INCREMENT,
  vault_id   BIGINT       NOT NULL,
  path       VARCHAR(512) NOT NULL,
  name       VARCHAR(255) NOT NULL,
  sha256     CHAR(64)     NOT NULL,
  size       BIGINT       NOT NULL,
  deleted    TINYINT      NOT NULL DEFAULT 0,
  version    BIGINT       NOT NULL,
  updated_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_vault_path (vault_id, path),
  KEY idx_vault_version (vault_id, version),
  KEY idx_vault_name (vault_id, name),
  KEY idx_vault_sha (vault_id, sha256)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

- `name` 单列出来加索引：`![[截图.png]]` 只能给文件名，解析时按它查，不必全表 LIKE。
- `size` 用于配额累加；墓碑行清 0，所以 `SUM(size)` 就是有效占用（`AttachmentMapper.sumSizeByVault`）。
- 墓碑不删磁盘 blob：同仓库其他路径可能仍在引用同一份内容。

---

## 3. 安全与限额

| 项 | 值 | 说明 |
| --- | --- | --- |
| 扩展名白名单 | png / jpg / jpeg / gif / webp / pdf | **刻意不含 svg** |
| 单文件上限 | 10MB | 流式读到超限即中断（413），不会先落盘再回滚 |
| 每仓库配额 | 1GB | 超限 413；默认值可配 |
| 附件限流 | 600/分钟/IP | 单独一桶，见第 8 节 |
| 路径校验 | 复用正文那套 | 库内相对路径、无 `.`/`..`/空段、无控制字符 |

- **不含 svg**：svg 能内联脚本，浏览器直出等于给了一条 XSS 通道。要支持就得先做 svg 清洗
  或强制 `Content-Disposition: attachment`，收益不值这个成本。
- Content-Type 由服务端**按扩展名映射**，不采信客户端传的类型；响应带
  `X-Content-Type-Options: nosniff`；`Content-Disposition` 用 RFC 5987 的
  `filename*=UTF-8''<pct-encoded>`（文件名整体编码，不拼裸引号，避免响应头注入）。
- 落盘用「临时文件 → `ATOMIC_MOVE`」，读方永远看不到半截文件；临时文件在 `finally` 里清理，
  被拒的上传（超限/超配额）不留下垃圾。
- 配额与单文件上限是**一次流式读取同时卡住**的：读取上限取 `min(单文件上限, 剩余配额 + 本路径旧占用)`，
  既不先把大文件写完再回滚，也不会因为粗粒度预检把「覆盖成更小的文件」误判成超配额。
- 目录权限 `0700`、文件 `0600`（Windows 开发机上无 POSIX 权限位，会被忽略，容器内生效）。
  这只是第二层——附件只经带 JWT 的接口下发，访问控制不靠文件系统。

配置项（`application.yml`，都可用环境变量覆盖）：

```yaml
daily-sync:
  storage:
    dir: ${ATTACHMENT_DIR:./data/attachments}   # 相对路径：本地 backend/ → backend/data/attachments
  attachments:
    max-file-bytes: 10485760     # 10MB
    quota-bytes: 1073741824      # 1GB / 仓库
  rate-limit:
    attachment-per-minute: 600
```

---

## 4. 插件端（quick-daily-note）

- **按引用发现附件，不按目录扫**：遍历同步范围内的 `.md`，取
  `metadataCache.resolvedLinks[md.path]` 的目标，排除正文（`.md`/`.json`）与白名单外的扩展名。
  这样无论用户用插件的 `pastedImageFolder` 还是 Obsidian 原生附件目录（`autoSavePastedImages`
  默认是关的，粘贴其实走 Obsidian 设置）都能覆盖；代价是**没被任何日记引用的图片不会上云**，
  这正是要的效果。
- 每设备状态新增两个表（都在本机 `data.json`，不入库）：
  - `attachmentHashes: path -> sha256`：上次同步的内容哈希；
  - `attachmentStamps: path -> "mtime:size"`：**纯省算力的旁路缓存**——附件动辄几 MB，
    每个周期把所有引用到的附件读一遍重算哈希太浪费，时间戳与大小都没变就跳过读取。
    它只影响性能：缺失或过期只会多算一次哈希，判断始终以 `attachmentHashes` 为准。
  - `normalizeSyncState` 必须**深拷贝**这两张表（与 `hashes` 同样的坑：浅拷贝会让多设备
    共享同一个默认表对象，一台设备的状态串到另一台）。
- 上传：哈希一致不发请求；发上去服务端对「同路径同内容」回 `unchanged` 且**不推进版本号**。
  超过 10MB 本地先挡一道，省一次注定 413 的请求。
- 拉取：`deleted` 且本地未改 → 进回收站；本地改过 → 保留并标记重传（与正文冲突策略完全一致）。
  下载**不**受引用集约束——服务端只会知道被某台设备的日记引用过的附件（各设备都只上传被引用的），
  所以云端列出什么就是该补的。
- **每轮下载上限 20 个 / 50MB**：新设备首次同步云端可能挂着上百张图，一口气拉完会让 Obsidian
  卡几分钟。被推迟的下一轮（5 分钟）继续，引用还在就不会漏，超限时给一次 Notice 说明。
- 事件钩子：非 `.md` 的 create/modify/delete/rename 也进队列（附件单独一条队列，因为是一条一个
  请求的原始字节上传），走同一个 3 秒防抖——刚粘贴完不用等 5 分钟周期。
  队列里的 `mod` 在上传前会按「当前引用集」过滤，防抖窗口内新粘贴还没引用的图不会被误传。
- 只删引用、文件还留着的**不**推墓碑（云端那份留着更保守，日后重新引用无需重传）；
  引用没了且本地文件也删了才推墓碑。

---

## 5. 网页端渲染

react-markdown 不认识 Obsidian 的写法，所以渲染前做一遍纯文本改写（`frontend/src/attachments.ts`）：

| 正文写法 | 处理 |
| --- | --- |
| `![[截图.png]]` | 改写成 `![](#ds-att:name=...)` → 内联图片 |
| `![[截图.png\|300]]` | 数值别名当显示宽度（`|300x200` 也认） |
| `![[文档.pdf]]` | 改写成链接 → 渲染成可点下载的胶囊 |
| `![](attachments/a.png)` | 相对路径交给服务端解析（精确未命中会按文件名兜底） |
| 反引号围栏块 / 行内 `` `code` `` | **跳过**，别把示例文本改成插图 |
| 白名单外的扩展名（如 `.svg`） | 原样保留文本——它没上云，改成图只会得到一张必然加载失败的图 |

- 用哨兵 `#ds-att:...` 而不是直接写接口地址：`<img>` 发不出 `Authorization` 头，
  接口地址塞进 `src` 只会拿到 401。哨兵是**纯相对 URL**，能通过 react-markdown 的
  `defaultUrlTransform`（它会剥掉 `ds-att://` 这类自定义协议，但放行相对 URL）。
- `MarkdownView` 的 `img`/`a` 组件认出哨兵 → 走 `api.blob()`（带 JWT 取字节）→ `URL.createObjectURL`。
- blob URL 按接口地址缓存（上限 200 条，FIFO 回收），避免每次重渲染/切页签都重新下载。
- 失败不静默裂图：显示「文件名 + 服务端原因 + 重试」。
- 新增两个 prop：`vaultId`（按 id 寻址）与 `recordPath`（相对路径与同目录优先）。
  `Records.tsx` 的两处调用点都已传；`App.tsx` 不需要改。

---

## 6. 部署：必须挂一个宿主机目录

`docker-compose.yml` 原来**没有任何 volume**（MySQL 是宿主机上独立容器，数据本来就在容器外，
所以这个问题此前没暴露）。附件落盘必须挂载：

```yaml
    volumes:
      - /opt/daily-sync/data:/app/data
```

- 这是 **bind mount，不是拷贝**：容器内 `/app/data` 与宿主机 `/opt/daily-sync/data` 是同一份数据，
  磁盘上只有一份。`docker inspect daily-sync --format '{{json .Mounts}}'` 显示
  `"Type":"bind"` 即确认。
- **不挂的后果是「零份」而不是「两份」**：附件写进容器可写层，而 `docker compose up -d`
  在镜像变化时是重建容器，可写层跟着没——图片静默消失（正文在 MySQL 里不受影响，所以现象是
  「正文在、图裂了」）。
- `/opt/daily-sync/data` 目录由 Docker 首次 `compose up` 自动创建，不需要手工 mkdir。
  上线前先确认 `/opt` 分区可用空间：`df -h /opt`。
- compose 文件由 `deploy.yml` 每次 push 覆盖到服务器（覆盖前留 `.bak`），所以写进仓库的这条
  下次部署自动生效；流水线里的 `docker image prune -f` 只清镜像、不动挂载目录。
- `.dockerignore` 加了 `backend/data/`、`data/`：Dockerfile 只 COPY `backend/pom.xml` 与
  `backend/src`，附件本来就不会烤进镜像，排除只是别让每次 build 都先传一坨数据给 daemon。
- 另把 `backend/data/`、`data/` 加进 `.gitignore`。

---

## 7. 验证记录（2026-09-15，全部通过）

**1) 接口手测（curl，新建测试账号 + 临时库）**

| 用例 | 结果 |
| --- | --- |
| 首次上传 | `stored`，version 1，sha256 与本地一致 |
| 同内容重复上传 | `unchanged`，**version 仍为 1**（幂等，不惊动其他设备） |
| 下载 | 200，字节与源文件 `cmp` 一致；`Content-Type: image/png`、`nosniff`、`ETag=sha256`、`filename*=UTF-8''` |
| 白名单外（.exe） | 400 `path: ... 只同步 gif / jpeg / jpg / pdf / png / webp` |
| 超 10MB | 413 `附件超过单文件上限 10MB`，且 `.tmp` 无残留 |
| `?name=` 解析 | 200（网页端 `![[a.png]]` 的用法） |
| 不给 path/name | 400 `缺少参数: path 或 name` |
| 墓碑删除 + 再删一次 | `deleted`（version 2）→ `unchanged`（version 仍 2） |
| 删除后下载 | 404 |

落盘布局确认为 `data/attachments/<vaultId>/<sha 前两位>/<sha256>`。

**2) 插件 headless 集成测试（`quick-daily-note/scripts/run-sync-test.mjs`）**

`42 通过，0 失败`（原有 32 项无回归 + 新增 10 项附件断言）。新增断言覆盖：
只上传被引用的附件（附件在日记文件夹之外也算）、云端 sha256 与本地一致、云端记录了文件名、
附件无变化的重同步是 no-op、空库设备拉到图片且**逐字节一致**、内容变化后重传更新 sha256、
删除后云端只剩墓碑、对端跟随删除进回收站。
另：obsidian 桩的 `requestUrl` 改成与真实行为一致（`text`/`json` 只在类型匹配时填、
恒返回 `arrayBuffer`），否则二进制下载的 bug 测不出来。

**3) 网页端渲染（浏览器实测，登录测试账号打开当天日记）**

| 检查 | 结果 |
| --- | --- |
| 三处引用（wiki 嵌入 / 带宽度别名 / 相对路径） | 都渲染成 `<img class="attachment-img">` |
| 图片是否真的解码出来 | `naturalWidth×naturalHeight = 871×289`，`src` 为 `blob:` |
| `\|200` 宽度别名 | 计算宽度正好 200px |
| pdf | 渲染成 `📎 说明.pdf` 胶囊，点击下载无报错 |
| 围栏块 + 行内代码里的 `![[image-2.png]]` | **未被替换**，原样显示文本 |
| 加载失败占位 / 错误提示 | 0 个 |

> 截图上会出现一层浏览器自身的命令浮层（「Quick Daily Note: 生成本周回顾」等），
> 那是宿主浏览器面板的 chrome，不在页面 DOM 里（已用 `document.body.innerText` 核对）。

---

## 8. 常见问题与坑

| 现象 | 原因与处理 |
| --- | --- |
| 发版后图片全没了 | 没挂 `/opt/daily-sync/data`（见第 6 节）。正文还在，只是图裂 |
| 网页端图片 401 / 裂图 | 不能用 `<img src="接口地址">`，必须走 blob（第 5 节）；已按此实现，若自己扩展接口注意别退回去 |
| 新设备首次同步很慢 | 附件按每轮 20 个 / 50MB 分轮下载，收敛即可；每轮结束会有一次 Notice 说明还剩多少 |
| 附件上传被 429 | 附件是**单独一桶**（600/分钟）。`/api/v1/sync` 是精确路径匹配，`/api/v1/sync/attachments` 不显式注册就完全不受限流保护——`WebMvcConfig` 里已加两条路径 |
| 粘贴了图但 5 分钟后才上云 | 附件引用要靠 `metadataCache` 解析，刚粘贴的瞬间可能还没索引到。md 改动会触发下一轮扫描，一般几秒内；实在慢就是等下一个周期 |
| 想放宽/收紧配额或类型 | 改 `application.yml` 的 `daily-sync.attachments.*`（或环境变量），白名单在 `AttachmentService.ALLOWED_EXTENSIONS`，**改完要同步改插件 `sync.ts` 的 `ATTACHMENT_EXTENSIONS` 与前端 `attachments.ts` 的 `ATTACHMENT_EXTENSIONS`**，三处必须一致 |
| 想临时关掉附件同步 | 没有独立开关；关掉插件自动同步即可。服务端删掉 `attachments` 表会让附件接口 500，别这么干 |
| 磁盘占用怎么查 | `du -sh /opt/daily-sync/data/attachments/*`（目录名就是 vaultId）；总占用同时受 1GB/仓库配额约束 |

### 与计划的偏差（都在这里说明）

1. **没有做逐条上传/删除的审计日志**（原计划加 `ATTACHMENT_UPLOAD` / `ATTACHMENT_DELETE`）。
   理由：正文推送本身就不逐条审计（只有 `VAULT_CREATE` 这类仓库级/身份级事件进审计），
   一次粘贴几十张图会把安全日志刷成流水账，反而降低它的价值。`AuditService` 与
   `AuditLogs.tsx` 因此未改。删用户时的附件清理照常做（见第 9 节）。
2. 上传协议由 multipart 改成原始字节流（第 1 节已说明理由）。
3. 计划外补了一处兜底：下载接口在精确 `path` 未命中时按文件名再找一次，兼容
   「相对笔记」与「相对库根」两种引用写法。
4. 计划外加了 `attachmentStamps` 旁路缓存（第 4 节），避免每个周期把所有附件读一遍重算哈希。

---

## 9. 运维

- **删用户**：`AdminService.deleteUser` 现在会一并删 `attachments` 行，并在**事务提交后**
  删掉该用户各仓库的附件目录。放在提交后是刻意的：若事务回滚，行还在而文件已没，
  附件就成了点不开的死链。附件按仓库分目录，逐个删既不会误伤别的用户，也不需要引用计数。
- **调配额/上限**：`daily-sync.attachments.max-file-bytes` / `quota-bytes`（见第 3 节配置块）。
- **看占用**：`du -sh /opt/daily-sync/data/attachments/*`。
- **孤儿 blob**：本期**不做回收**。删除只落墓碑、盘上文件保留（同仓库内多路径可共享同一份 blob，
  需要引用计数才敢删）。将来要做时按仓库扫即可，不必全局扫——这也是按仓库分目录的收益之一。

---

## 10. 明确不做 / 下一步

- 孤儿 blob 的引用计数 GC（理由同上）
- 跨仓库 blob 去重（按仓库分目录的有意取舍）
- 附件版本历史与回滚
- 网页端上传/删除附件（正文仍只读，附件只随插件同步进来）
- 支持 svg / 更大文件 / 更多类型（要动就用第 8 节的「三处白名单必须同步改」清单）

下一步候选（沿用 M6 第 8 节）：

1. **发版顺序**：先把后端部署上去（push master 触发 Actions）并确认 `/opt/daily-sync/data`
   已挂载生效，再发插件新版本——插件开始推附件时服务端必须已经有这四个接口，
   否则会一路 404（正文同步不受影响，但附件会一直被跳过）。
2. nginx + 域名 + HTTPS + `BEHIND_PROXY=true`
3. 前端「同步设置引导页」
